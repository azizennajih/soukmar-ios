# SoukMar iOS — Claude Working Guide

## Projekt

Native iOS-App (SwiftUI) für SoukMar, spiegelt [[soukmar]] (Angular-Web) und [[soukmar-android]] (Kotlin/Compose) — gleicher Backend-Vertrag (`soukmar-backend`, `http://<host>:3000/api/`), gleiche Feature-Phasen, aber eigenständiges Xcode/Swift-Projekt.

**Pfad:** `C:\AIProjekte\soukmar-ios`
**GitHub:** github.com/azizennajih/soukmar-ios
**Stack:** SwiftUI (iOS 16+), `async`/`await` + `URLSession` (kein Alamofire o.ä. — bewusst schlank gehalten wie Androids reines Retrofit-Setup), Keychain für Session-Persistenz.

---

## Besonderheit dieses Projekts: kein lokaler Mac

Diese gesamte Session läuft auf Windows — **es gibt keinen lokalen Mac und kein Xcode**. Das bedeutet:

1. **Kein `xcodeproj`/`xcworkspace` wird committed.** Stattdessen beschreibt `project.yml` ([XcodeGen](https://github.com/yonaskolb/XcodeGen)-Spec) die Projektstruktur als lesbares YAML; `xcodegen generate` erzeugt daraus das eigentliche Xcode-Projekt — sowohl lokal auf einem echten Mac als auch in der CI. Neue Dateien/Targets/Settings **immer in `project.yml` ändern**, nie ein `.xcodeproj` von Hand pflegen (existiert hier gar nicht im Repo).
2. **Verifikation läuft ausschließlich über Codemagic CI** (`codemagic.yaml`), nicht über einen lokalen Build+Simulator-Lauf wie bei Android (dort: Gradle + adb + Emulator, alles lokal möglich). Nach jeder Code-Änderung: committen, pushen, den Codemagic-Build abwarten/prüfen (`gh` oder die Codemagic-Weboberfläche), Fehler beheben, erneut pushen. **Kein Build = keine Verifikation** — Code, der nie durch Codemagic gelaufen ist, gilt als ungetestet.
3. Der Nutzer testet die fertige App auf einem **iPad** — dafür entweder (a) TestFlight (braucht ein bezahltes Apple Developer Program-Konto, aktuell **nicht eingerichtet**) oder (b) einen gemieteten Cloud-Mac mit Fernzugriff. Bis das Apple-Konto steht, validiert der `simulator-build`-Workflow in `codemagic.yaml` nur, dass der Code kompiliert (kein Code-Signing, kein Gerät, keine TestFlight-Auslieferung).

## Codemagic-Setup (einmalig, nur der Nutzer kann das tun)

1. Auf [codemagic.io](https://codemagic.io) mit dem GitHub-Account einloggen (kostenloses Basic-Tier reicht für den Anfang).
2. Repo `azizennajih/soukmar-ios` verbinden — Codemagic erkennt `codemagic.yaml` automatisch.
3. Workflow `simulator-build` manuell starten (oder auf Push automatisch triggern lassen, in den Codemagic-Workflow-Einstellungen konfigurierbar).
4. Ergebnis prüfen: **BUILD SUCCESSFUL** bestätigt, dass der aktuelle Codestand kompiliert — mehr nicht (kein Signing, kein Simulator-Lauf, keine UI-Tests bisher).

Sobald ein Apple Developer Program-Konto existiert: zweiten Workflow (`device-build` o.ä.) mit Code-Signing (App Store Connect API Key in den Codemagic-Umgebungsvariablen hinterlegen) + `publishing.app_store_connect` für TestFlight ergänzen — noch nicht umgesetzt.

---

## Architektur-Konventionen

- **Keine externen Networking-Libraries** — reines `URLSession` + `async/await` (`Networking/APIClient.swift`), mirrort Androids Retrofit-Schicht 1:1 im Vertrag (gleiche Endpunkte, gleiche Fehlerform `{error, unverified?}` → `ApiErrorDto`).
- **MVVM**: `ObservableObject`-ViewModels (`@Published`-Properties) pro Screen, analog zu Androids Hilt-ViewModels — hier ohne DI-Framework, `XRepository.shared`-Singletons genügen für den aktuellen Umfang.
- **Session-Persistenz**: `Persistence/TokenStore.swift`, Keychain für den JWT (sicherer Default als Androids DataStore-Preferences, die dort auch nicht verschlüsselt sind — bewusst besser gemacht, kein Nachteil).
- **Models**: `Codable`-Structs unter `Models/`, Feldnamen exakt wie in `soukmar-backend`/Androids DTOs (z. B. `UserDto`, `LoginRequest`) — bei neuen Endpunkten immer zuerst die Android-DTOs oder die Backend-Route lesen, nicht neu raten.
- **Design-Farben**: `Views/CommonComponents.swift`'s `Color.soukmar*`-Extension — exakt dieselbe Palette wie `soukmar-android/.../theme/Color.kt` und `soukmar/src/styles.scss` (`--primary` = `#D93D4A` etc.). Bei Änderungen alle drei synchron halten.
- **i18n**: noch nicht portiert (kommt in einer späteren iOS-Phase, mirrort Androids Phase-13-Ansatz: Web-JSONs 1:1 als Bundle-Resources übernehmen, keine eigene Übersetzungspflege).

---

## Phasenplan (mirrort soukmar-android, aber eigenständig durchnummeriert)

Reihenfolge und Stand:

1. ✅ **Auth (Login, Register, Passwort vergessen)** — `Views/LoginView.swift`, `RegisterView.swift`, `ForgotPasswordView.swift` + zugehörige ViewModels, echte Push-Navigation zwischen den dreien via `RootView.swift`'s `AuthFlowView` (`NavigationStack` + Enum-Route, mirrort Androids Login/Register/ForgotPassword-Nav-Graph-Einträge). **ResetPassword fehlt noch** (braucht einen per E-Mail zugestellten Deep-Link-Token — Universal Links/Deep-Linking für iOS ist eigene Recherche, noch nicht angegangen). **Codemagic-Build #2 bestätigt grün** (Commit `d2b2cbe`, 1m 11s).
2. ✅ **Home + Anzeigen durchsuchen/suchen** — `Views/HomeView.swift` (Begrüßung, Such-Einstieg, Kategorien-Grid aus `Models/CatalogModels.swift`s `CATEGORIES`, Push-Navigation via eigenem `Route`-Enum wie in `RootView`), `Views/ListingsView.swift` (Kategorie-/Zustand-Chips, Suchleiste, Filter-Sheet mit Preis-Range + dynamischen EAV-Attributen aus `catalog/categories/{category}/full`, paginiertes Grid via `onAppear`-Trigger auf der letzten Karte), `Views/ListingCardView.swift`, `ViewModels/ListingsViewModel.swift` (spiegelt Androids `ListingsViewModel.kt` 1:1: `buildParams()` mit exakt denselben Query-Keys `q`/`category`/`subcategoryId`/`condition`/`minPrice`/`maxPrice`/`attr_<CODE>`/`attr_<CODE>_min`/`_max`), `Models/ListingModels.swift`, `Networking/ListingRepository.swift` + `CatalogRepository.swift`, `APIClient`s neuer Query-Param-`send(path:query:)`-Overload. **Bewusst ausgelassen** (spätere iOS-Phase, Androids Phase-10-Äquivalent): gespeicherte Suchen, Sortierung (`tri`), Karten-Tap navigiert noch nirgends hin (Anzeigendetail ist Phase 3). **Wichtige Vorsichtsmaßnahme ohne lokalen Compiler**: Swifts automatisch generiertes `Decodable` ignoriert Property-Defaults bei fehlendem JSON-Key (anders als Kotlinx auf Android) — `ListingDto`, `ListingsResponseDto`, `AttributeDefinitionDto`, `SubcategoryWithAttributesDto`, `CategoryFullResponse` haben deshalb manuelle `init(from:)` mit `decodeIfPresent(...) ?? default`, gegen das Backend (`soukmar-backend/src/routes/listings.ts`s `sanitizeListing()`) verifiziert, das `region`/`phone`/`whatsapp` je nach Eigentümer/`showPhone` komplett aus der Response entfernt. **Codemagic-Build #3 bestätigt grün** (Commit `eac0b79`) — reiner Kompilier-Check, echtes Durchklicken der Filter/Pagination steht noch aus (kein Simulator-Lauf möglich, siehe oben).
3. ✅ **Anzeigendetail** — `Views/ListingDetailView.swift` (Bilder-Galerie via `TabView(.page)`, Preis mit Ø-Preis-Vergleichsbadge (`priceComparisonPct`, 📉/📈 wie im Web), Kategorie-Badge, Beschreibung, dynamische EAV-Attributliste (`SpecRow`-Äquivalent: BOOLEAN→Oui/Non, NUMBER formatiert, SELECT/TEXT via `humanizeCode()`), Favoriten-Herz + Melden-Flag in der Toolbar (nur eingeloggt), Bewertungsformular (Sterne 1–5 + Kommentar, nur wenn `canReview` vom Backend bestätigt), Melden-Sheet mit 10-Zeichen-Mindestlänge), `ViewModels/ListingDetailViewModel.swift` (spiegelt Androids `ListingDetailViewModel.kt`), neue `Networking/ReviewRepository.swift` + `ReportRepository.swift`, `Models/ReviewModels.swift` + `ReportModels.swift` + `CommonModels.swift`, `ListingRepository` um `getFavoriteIds()`/`addFavorite()`/`removeFavorite()` ergänzt. Navigation: `ListingsView` pusht jetzt per `NavigationLink(value: listing.id)` + `.navigationDestination(for: String.self)`. **Bewusst ausgelassen** (spätere iOS-Phasen): "Contacter le vendeur" (Chat ist Phase 5), Verkäuferprofil-Link (Phase 9). **Wichtiger Navigation-Fix dabei gefunden**: `HomeView`s `NavigationStack` nutzte einen streng typisierten `[Route]`-Pfad — das trägt aber nur *einen* Zieltyp für den ganzen Stack; da `ListingsView` jetzt zusätzlich einen `String` (Listing-ID) pusht, wurde auf `NavigationPath` (type-erased) umgestellt, sonst hätte der Karten-Tap zur Laufzeit vermutlich lautlos nicht navigiert — ohne Simulator nicht zur Laufzeit prüfbar, daher vorsorglich korrekt gemacht statt erst bei echtem Testen zu entdecken — **erster Versuch schlug trotzdem fehl** (Commit `a3fdd2c`): `NavigationPath.append()` ist generisch über `Hashable`, weshalb die Kurzschreibweise `.listings(category: nil)` nicht mehr zu `Route` aufgelöst werden konnte (`type 'Decodable' has no member 'listings'`) — beim alten, streng typisierten `[Route]`-Array ging das noch, weil `Array.append(_:)` nicht generisch über den Elementtyp selbst ist. Gefixt durch explizites `Route.listings(...)` (Commit `bae08a3`). **Codemagic-Build #5 bestätigt grün** (Commit `bae08a3`).
4. ✅ **Anzeige aufgeben** — `Views/DeposerAnnonceView.swift` (5-Schritt-Assistent Kategorie→Sous-catégorie→Détails→Photos→Contact, mirrort `DEPOSER_STEPS`), `ViewModels/DeposerAnnonceViewModel.swift` (spiegelt Androids `DeposerAnnonceViewModel.kt` 1:1, inkl. `PhotoItem` mit lokalen vs. bereits hochgeladenen Bildern, `canNext`-Validierung pro Schritt, `maxPhotos` 10/20 je nach `isPremium`-Toggle). Fotoauswahl nutzt SwiftUIs natives `PhotosPicker` (iOS 16+) statt Androids `ActivityResultContracts.GetMultipleContents()`. Neue `Models/ListingUpsertModels.swift` (`AttrValue`-Enum text/bool — NUMBER-Attribute reisen als String wie bei Android, das Backend coerced serverseitig via zod), `Networking/UploadRepository.swift` (neuer `APIClient.upload(path:fieldName:files:)`-Multipart-Helper, `POST /api/upload` Feldname `images`), `ListingRepository` um `createListing()`/`updateListing()` ergänzt. Einstieg: neuer Floating-Action-Button (+) auf `HomeView`. Nach erfolgreichem Publizieren: `path.removeLast()` + `path.append(newListingId)` (ersetzt den Assistenten durch die Anzeigendetailseite, mirrort Androids `popUpTo(DEPOSER_ANNONCE){inclusive=true}`). **Bewusst ausgelassen**: Edit-Einstieg (ViewModel unterstützt `start(editId:)` bereits, aber kein UI-Einstieg — kommt mit Phase 6 "Mes Annonces", exakt wie bei Android). **Wichtiger Navigation-Fix dabei gefunden**: `.navigationDestination(for: String.self)` (Listing-Detail) war bisher auf `ListingsView` selbst deklariert — das wirkt aber nur, solange diese View Teil des aktiven Stacks ist. Da der neue FAB-Weg (Home→Assistent→Detail) `ListingsView` nie durchläuft, wurde die Registrierung auf `HomeView`s `NavigationStack`-Wurzel verschoben (neben der schon vorhandenen `Route.self`-Registrierung), damit jeder Pfad, der eine Listing-ID pusht, sie findet. **Codemagic-Build #6 bestätigt grün** (Commit `1d2fa9e`) — reiner Kompilier-Check, echtes Durchklicken des Assistenten (inkl. `PhotosPicker`-Berechtigungen, Foto-Upload) steht noch aus.
5. ✅ **Chat (Socket.IO)** — erste externe Abhängigkeit im Projekt: `project.yml` bekam einen `packages:`-Eintrag (XcodeGen-SPM-Syntax) für `socketio/socket.io-client-swift` (v16.1.1, zieht Starscream transitiv), `codemagic.yaml` um einen expliziten `xcodebuild -resolvePackageDependencies`-Schritt + höheres `max_build_duration` (45 statt 30 Min.) ergänzt. `Networking/ChatSocketManager.swift` spiegelt Androids `ChatSocketManager.kt` 1:1 (gleiche Events `new_message`/`offer_updated`/`user_typing`/`listing_status_changed`, gleiche Emits `join_conversation`/`send_message`/`send_offer`/`respond_offer`/`cancel_offer`/`cancel_reservation`/`typing`). **Wichtige Rechercheerkenntnis**: socket.io-client-swift hat **kein** `.auth`-Config-Case (nur `.connectParams` → landet in `handshake.query`, nicht `handshake.auth`, welches das Backend explizit prüft) — die korrekte Methode ist `socket.connect(withPayload: ["token": token])`, das den Socket.IO-CONNECT-Paket-Payload sendet und serverseitig exakt als `socket.handshake.auth` ankommt; verifiziert durch Lesen des Bibliotheks-Quellcodes (`SocketIOClient.swift`, `SocketTypes.swift` — `extension Dictionary: SocketData` ist unconstrained) direkt von GitHub, **keine Backend-Änderung nötig**. `ViewModels/ChatListViewModel.swift` + `ChatViewModel.swift` spiegeln Androids Pendants (inkl. Typing-Debounce via `Task.sleep`), `Views/ChatListView.swift` + `ChatView.swift` (Text-/Angebots-/System-Bubbles, Schnellantworten, Angebots-Eingabe, Melden-Sheet, Reservierung-stornieren/Angebot-stornieren-Alerts). **Bewusste Vereinfachung ggü. Android**: Android verbindet/trennt den Socket exakt mit dem Lebenszyklus des Chat-Listen-Screens (ViewModel erstellt/gelöscht); SwiftUIs `NavigationStack` feuert `onDisappear` aber schon beim bloßen Überdecken durch einen gepushten Screen (nicht nur beim Pop) — ein `onDisappear`-Disconnect hätte die Verbindung sofort beim Öffnen einer Konversation gekappt. Stattdessen: `connect()` lazy bei jedem Chat-Öffnen (idempotent), `disconnect()` nur beim Logout. Schließt außerdem die in Phase 3 bewusst ausgelassene Lücke: `ListingDetailView` hat jetzt eine vollständige Kontakt-Karte (Telefon, WhatsApp-`Link`, "Contacter le vendeur" → `ChatRepository.startConversation()` → Navigation zur Konversation). **Neuer `NavigationPath`-Baustein**: `ConversationRoute` (dediziertes `Hashable`-Wrapper-Struct statt eines rohen `String`) für Konversations-IDs, da `HomeView`s Stack bereits rohe `String`-Pushes für Listing-IDs registriert hat — mirrort exakt die in Phase 4 dokumentierte Lektion ("zweite String-Bedeutung braucht einen eigenen Typ"). Beide `navigationDestination(for:)`-Registrierungen (String für Listing-IDs, `ConversationRoute` für Konversationen) sitzen an `HomeView`s Stack-Wurzel, nicht auf Blattscreens. **Codemagic-Build #7 bestätigt grün** (Commit `b1ae300`) — SPM-Paketauflösung (`socket.io-client-swift` + transitiv Starscream) hat beim ersten Versuch funktioniert, keine Nacharbeit nötig. Weiterhin nur Kompilier-Check — ein echter Socket.IO-Verbindungsaufbau/Nachrichtenaustausch gegen den Backend-Server wurde noch nicht getestet (kein Simulator verfügbar).
6. ✅ **Meine Anzeigen** — `Views/MesAnnoncesView.swift` (Karten mit Thumbnail/Status-Badge/Preis/Views-Zeit-Stadt, Aktionsleiste Voir/Réserver-Toggle/Modifier/Bump/Statistiken/Supprimer als SF-Symbol-Buttons, eingebettetes 14-Tage-Balkendiagramm), `ViewModels/MesAnnoncesViewModel.swift` (spiegelt Androids `MesAnnoncesViewModel.kt`: 24h-Bump-Cooldown-Berechnung, optimistisches Reserve/Active-Toggle mit Rollback bei Fehler, lazy geladene Stats pro Anzeige). `ListingRepository` um `getMyListings()`/`getViewStats()`/`bump()`/`updateStatus()`/`deleteListing()` ergänzt (neue `ViewStatDayDto`/`ViewStatsDto`/`ListingStatusUpdateRequest` in `ListingModels.swift`). **Nutzt den in Phase 4 bereits vorbereiteten Edit-Modus**: `DeposerAnnonceView`/`-ViewModel` unterstützten `editId` schon seit Phase 4 ohne UI-Einstieg — der fehlte jetzt, `HomeView`s `Route.newListing` wurde zu `Route.listingForm(editId: String?)` verallgemeinert (FAB pusht weiterhin `editId: nil`, "Modifier" in Mes-Annonces pusht `editId: id`); der bestehende Publish-Navigationsfluss (`path.removeLast()` + `path.append(newListingId)`) funktioniert für Create **und** Edit identisch, keine Sonderbehandlung nötig (mirrort Androids `popUpTo(DEPOSER_ANNONCE){inclusive=true}`, das ebenfalls für beide Fälle gleich ist). Einstieg: neues Listen-Icon im `HomeView`-Toolbar (führend, neben dem Chat-Icon). **Bewusste Vereinfachung**: Toast-Fehlermeldungen nutzen eine einfache auto-verschwindende Bottom-Banner-Overlay statt einer echten Snackbar-Queue wie Android — bei sehr schnell aufeinanderfolgenden Fehlern kein garantiert unabhängiger Pro-Nachricht-Timer, aber für die seltenen Fehlerfälle hier ausreichend. **Codemagic-Build #8 bestätigt grün** (Commit `065838f`).
7. ✅ **Favoriten** — `Views/FavorisView.swift` (2-Spalten-Grid aus `ListingCardView`, Herz-Overlay oben rechts zum direkten Entfernen), `ViewModels/FavorisViewModel.swift` (spiegelt Androids `FavorisViewModel.kt`: optimistisches Entfernen mit Rollback bei Fehler). `ListingRepository` um `getFavorites()` ergänzt (`getFavoriteIds()` nutzt sie jetzt intern statt eines eigenen Requests). Einstieg: neues Herz-Icon im `HomeView`-Toolbar (führend, dritte Position nach Chat/Mes-annonces). Kürzeste Phase bisher — reine CRUD-Liste ohne neue Muster. **Codemagic-Build #9 bestätigt grün** (Commit `99de9e4`).
8. ✅ **Profil** — `Views/ProfilView.swift` (Avatar mit `PhotosPicker`-Kamera-Overlay, Identitäts-Karte inkl. Admin-Badge, Formular Name/E-Mail-readonly/Telefon/Ville, Passwort-ändern-Formular), `ViewModels/ProfilViewModel.swift` (spiegelt Androids `ProfilViewModel.kt` 1:1). `AuthRepository` um `updateProfile()`/`updateProfileImage()`/`changePassword()` ergänzt (neue `ProfileUpdateRequest`/`ProfileImageUpdateRequest`/`ChangePasswordRequest` in `AuthModels.swift` — bewusst zwei getrennte Update-DTOs wie bei Android, da `PUT auth/profile` nur die im Body vorhandenen Felder anfasst; ein kombiniertes DTO hätte bei einem reinen Profil-Save das Avatarbild mit `null` überschrieben). **Home-Toolbar aufgeräumt**: Profil + "Se déconnecter" sitzen jetzt hinter einem `Menu` (⋯-Icon) statt als lose Icons — mirrort exakt Androids eigene Phase-8-Bereinigung ("Profil+Logout stecken jetzt hinter einem ⋮-Overflow-Menü"), rein zufällige Parallel-Entscheidung, nicht bewusst kopiert, aber schön konsistent. **Codemagic-Build #10 bestätigt grün** (Commit `4e6941e`).
9. ✅ **Verkäuferprofil** — `Views/SellerProfileView.swift` (Avatar, Sternebewertung als reine `★`-Glyphen, Antwortzeit-Badge dreistufig wie Android (`< 1h`/`< 24h`/sonst), Mitglied-seit-Datum via `DateFormatter` mit `fr_FR`-Locale, 2-Spalten-Grid aktiver Annoncen via `ListingCardView`, Bewertungsliste), `ViewModels/SellerProfileViewModel.swift` (spiegelt Androids `SellerProfileViewModel.kt`: paralleler Ladevorgang via `async let` für Listings + Reviews, nur der Profil-Load selbst ist fatal bei Fehler). Neue `Networking/UserRepository.swift` (`GET users/{id}/profile`, `GET users/{id}/listings`), `ReviewRepository` um `getForUser()` ergänzt, neue `SellerProfileDto`/`ReviewWithDetailsDto`/`ReviewAuthorDto`/`ReviewListingRefDto`/`ReviewsForUserResponse` in `ReviewModels.swift`. **Schließt eine Lücke aus Phase 3**: `ListingDetailView` hatte bisher gar keine Verkäufer-Karte — jetzt mit Name/Ville/"Voir ses annonces"-Button (`onOpenSeller`-Closure, analog zu `onOpenConversation` aus Phase 5). Neuer `SellerRoute`-Hashable-Wrapper (dritter Konversations-artiger Wrapper nach `String` für Listings und `ConversationRoute`), Registrierung wie gehabt an `HomeView`s Stack-Wurzel. **Codemagic-Build #11 bestätigt grün** (Commit `6f89e6d`).
10. ✅ **Gespeicherte Suchen** — `Views/SavedSearchesView.swift` (Liste mit Kategorie/Ville/Preis-Zusammenfassung, Tap öffnet `ListingsView` mit wiederhergestellten Filtern, ×-Button zum Löschen), `ViewModels/SavedSearchesViewModel.swift` + neue `Networking/SavedSearchRepository.swift` (`GET`/`POST`/`DELETE saved-searches`) + `Models/SavedSearchModels.swift`. `ListingsViewModel` um `saveSearch()`/`cancelSaveSearch()`/`applySavedSearchById()` ergänzt (spiegelt Androids Verhalten exakt: **kein City-Filter** wird beim Speichern je gesetzt, da `ListingsViewModel` — wie bei Android bewusst entschieden — überhaupt kein Stadt-Filterfeld hat; NUMBER-Attribut-Ranges werden wie im Web/Android nicht persistiert, nur SELECT/BOOLEAN-Auswahlen). "🔔 Enregistrer cette recherche" sitzt in `ListingsView` direkt unter der Kategorie-/Zustand-Chip-Zeile, nur sichtbar wenn `TokenStore.shared.isLoggedIn`. Eine gespeicherte Suche erneut öffnen läuft über `Route.listings(category:, savedSearchId:)` → `ListingsView(savedSearchId:)` → `ListingsViewModel.applySavedSearchById()` (kein GET-by-id-Endpoint, lädt die volle Liste und filtert lokal, exakt wie Android). Einstieg: neuer Menüpunkt "Recherches sauvegardées" im `HomeView`-Overflow-Menü. **Nachträglich per Codemagic-Build bestätigt** (mitverifiziert durch den Tranche-1-Build, Commit `3279bd6` — siehe "Web-Sync-Tranchen" unten).
11. ✅ **Benachrichtigungen** — analog zu Androids eigener Aufteilung in zwei Teile:
    - **In-App-Benachrichtigungen** (voll funktionsfähig): `Views/NotificationsView.swift` (Liste, ungelesen-Punkt + Hervorhebung, "Tout marquer comme lu", Klick-Routing pro Typ: `NEW_INQUIRY`/`NEW_REPLY`/`NEW_MESSAGE`→Chat, `NEW_REVIEW`→eigenes Profil, `SAVED_SEARCH_MATCH`→Anzeigendetail, `REPORT_RESOLVED`→keine Navigation), `ViewModels/NotificationsViewModel.swift`, neue `Networking/NotificationRepository.swift` (`GET notifications`, `GET notifications/unread-count`, `PATCH notifications/{id}/read`, `PATCH notifications/read-all`) + `Models/NotificationModels.swift`. Glocke mit Badge (rundes Zahlen-Overlay) im `HomeView`-Toolbar, Unread-Count wird per `.task { while !Task.isCancelled { ...; Task.sleep(30s) } }` alle 30s gepollt, mirrort exakt Webs/Androids Navbar-Poll-Ansatz (kein Live-Update-Mechanismus im Backend für das Badge). Französische Templates hardcoded (kein i18n-Layer), 1:1 aus `fr.json` übernommen.
    - **Echter Push (APNs) bewusst NICHT umgesetzt**: dafür fehlen zwei Voraussetzungen gleichzeitig — (1) die "Push Notifications"-Capability braucht ein App-ID-Setup im **bezahlten** Apple Developer Program (noch nicht eingerichtet, siehe oben), (2) das Backend spricht aktuell nur FCM (`push/fcm-register`, und selbst das läuft noch mit einem Platzhalter-Firebase-Projekt bei Android) — für iOS bräuchte es zusätzlich einen APNs-Auth-Key + eigene Registrierungs-Route. Genau dieselbe "strukturell vorbereitet, aber ohne echte Credentials nutzlos"-Situation wie Androids FCM-Lücke, nur dass hier noch nicht einmal das Grundgerüst (Berechtigungsanfrage, Token-Registrierung) angelegt wurde, da es ohne Backend-Gegenstück nichts zu tun gäbe — bewusst kein totes/irreführendes Code-Grundgerüst geschrieben. **Codemagic-Build #12 bestätigt grün** (Commit `87f3a9e`).
12. ✅ **Admin (Meldungen)** — `Views/AdminView.swift` (Filter-Pills mit Live-Zählern PENDING/RESOLVED/DISMISSED/ALL, Report-Karten mit Reporter/Reported/Anzeigenlink/Grund/interner Notiz, Resolve/Rejeter via `.alert(...)` mit eingebettetem `TextField` für die optionale Notiz — SwiftUI-Alerts unterstützen Texteingaben nativ, kein Custom-Dialog nötig), `ViewModels/AdminViewModel.swift` (spiegelt Androids `AdminViewModel.kt` 1:1, inkl. des Selective-Field-Merge-Bugfixes aus Androids Historie: `PATCH reports/admin/{id}` liefert keine `reporter`/`reported`/`listing`-Relationen, also werden nur `status`/`adminNote`/`resolvedAt` aus der Antwort übernommen statt die ganze Zeile zu ersetzen). Neue `Networking/AdminRepository.swift` (`GET`/`PATCH reports/admin`), `AdminReportDto`/`AdminReportUpdateRequest`/`ReportUserRefDto`/`ReportListingRefDto` in `ReportModels.swift`. **Gleiche bewusste Inkonsistenz wie Android/Web**: Menüpunkt nur für `role == "ADMIN"` sichtbar, obwohl das Backend zusätzlich `MODERATOR` erlaubt — Web-Navbar macht es genauso, iOS übernimmt das bewusst statt es zu "fixen". Einstieg: neuer, bedingt sichtbarer Menüpunkt "Signalements" im `HomeView`-Overflow-Menü. **Scope wie bei Android**: nur der Signalements-Tab, die anderen Web-Admin-Tabs (Aperçu/Annonces/Utilisateurs/Revenus) haben keinen funktionierenden Backend-Contract. **Codemagic-Build #13 bestätigt grün** (Commit `dd969ee`).
13. ✅ **i18n (6 Sprachen)** — letzte Phase des Plans, alle 13 Phasen jetzt umgesetzt. **Architektur identisch zu Android**: die 6 Web-JSON-Dateien (`fr`/`en`/`ar`/`de`/`es`/`it`) wurden 1:1 unverändert nach `Resources/i18n/*.json` kopiert (XcodeGen bündelt lose Nicht-Swift-Dateien unter `sources:` automatisch als Bundle-Resources, keine `project.yml`-Änderung nötig) und zur Laufzeit geparst — Single Source of Truth bleibt das Web-Repo. Neues `I18n/I18nRepository.swift`: eine `ObservableObject`-Singleton-Klasse (`I18nRepository.shared`) statt Androids CompositionLocal — jede SwiftUI-View mit `@ObservedObject private var i18n = I18nRepository.shared` rendert automatisch neu, wenn sich `currentLang` (`@Published`) ändert, ganz ohne Androids manuellen "touch currentLang for recomposition"-Kniff. Gleiche Lookup-Semantik wie Android/Web: gepunkteter Key → verschachtelte Objekt-Traversierung (`t("namespace.key")`), `{param}`-Interpolation, fehlender Key → roher Key als Fallback, `tCatalog(key, code:)` für Backend-Katalogcodes mit Fallback auf `humanizeCode()`, `timeAgoT()` für lokalisierte relative Zeit (gleiche bekannte Lücke wie Android: "gerade eben"/"Monate" bleiben mangels JSON-Key immer Französisch). Sprachwahl persistiert in `UserDefaults` (Key `soukmar_lang`, unabhängig vom Keychain-Token, übersteht Logout). **RTL**: `SoukMarApp.swift` setzt `.environment(\.layoutDirection, i18n.isRtl ? .rightToLeft : .leftToRight)` an der Wurzel — SwiftUI spiegelt die gesamte Subtree automatisch (Nav-Bars, Alignment etc.), exakt wie Androids `LocalLayoutDirection`-Override. `Views/LanguageSwitcher.swift` (Flagge+Code-Button mit `Menu`-Dropdown, alle 6 Sprachen) sitzt auf `LoginView` und im `HomeView`-Toolbar, wie bei Android.
    - **Migrationsstrategie**: Kern-Infrastruktur + Auth-Screens (Login/Register/ForgotPassword) + `HomeView` + `NotificationsView` + `AdminView` direkt umgesetzt (letztere beide waren in Phase 11/12 schon mit den korrekten französischen Strings geschrieben worden, mussten nur noch auf `i18n.t(...)` umgehängt werden). Die übrigen 11 Screens (`ListingsView`, `ListingDetailView`, `ListingCardView`, `DeposerAnnonceView`, `ChatListView`, `ChatView`, `MesAnnoncesView`, `FavorisView`, `ProfilView`, `SellerProfileView`, `SavedSearchesView`) über drei parallele Subagenten migriert — anders als bei Android gibt es dabei **kein Gradle-Race-Risiko**, da kein lokaler Compiler existiert und keiner der Agenten selbst baut; jeder Agent bekam die exakt analoge, bereits migrierte Android-Kotlin-Datei als Referenz genannt (Android hat die Key-Zuordnung schon einmal gemacht, `t("key")`-Aufrufe dort verraten exakt, welcher Key zu welchem String gehört — deutlich zuverlässiger als blindes Rätselraten in `fr.json`). Alle drei Agenten-Ergebnisse wurden anschließend gegen `fr.json` gegengeprüft (jeder verwendete Key tatsächlich vorhanden) — keine erfundenen Keys gefunden.
    - **Bekannte, bewusst dokumentierte Lücken** (kein Blocker, mirrort exakt Androids eigene Lückenliste): Backend-Fehlermeldungen bleiben Französisch (kein `Accept-Language`); vereinzelte Strings ohne Web-Pendant bleiben hardcodiert Französisch (z. B. "Bonjour, {name}"-Begrüßung, `.navigationTitle("Annonce")`/`"Profil vendeur"`/`"Signaler"`, "Conversation introuvable.", die 4 iOS-spezifischen `CONDITION_OPTIONS`-Chip-Labels ohne vollständige JSON-Abdeckung für das 4-Werte-Modell); `DEPOSER_STEPS` in `DeposerAnnonceViewModel.swift` blieb ein rohes französisches Array (die UI selbst liest die Schritt-Labels stattdessen über ein lokales `stepKeys`-Array in `DeposerAnnonceView.swift`, das denselben Index nutzt). **Bewusst kein Accessibility-Label-Sweep**: Icon-only-Buttons (Chat/Mes-annonces/Favoris/Notifications im `HomeView`-Toolbar) behalten unübersetzte `contentDescription`-Äquivalente, wie auch bei Android dokumentiert ("Retour"-contentDescriptions durchgängig Französisch).
    - **Nachträglich per Codemagic-Build bestätigt** (mitverifiziert durch den Tranche-1-Build, Commit `3279bd6` — siehe "Web-Sync-Tranchen" unten) — war zuvor trotz größtem Diff aller Phasen (18 Dateien geändert/neu) ungetestet geblieben.

**Vor jeder neuen Phase**: die passende Web-Implementierung (`soukmar/`) UND die bereits fertige Android-Implementierung (`soukmar-android/`) lesen — Android hat die ganze Recherche- und Edge-Case-Arbeit schon einmal gemacht (Backend-Endpunkte, exakte Feldnamen, Business-Regeln), das erspart erneutes Nachschlagen im Backend-Code.

### Web-Sync-Tranchen (nach Phase 13, laufend)

Wie bei Android wächst das Projekt ab hier nicht mehr in nummerierten Phasen, sondern in gebündelten **Tranchen**, die den Rückstand ggü. `soukmar/` (Web) aufholen — Android hat diesen Rückstand bereits in 9 Tranchen aufgeholt (siehe `soukmar-android/CLAUDE.md`s "Web-Sync-Runden"), iOS zieht jetzt nach demselben Muster nach, bewusst in kleineren Häppchen als Android, weil hier kein lokaler Compiler existiert (siehe oben) — jeder Fehler wird erst nach einem Codemagic-Push sichtbar, also lieber kleinere, risikoärmere Bündel.

- **Tranche 1**: (1) **Kritischer Bugfix**: `POST /auth/register` lehnt seit einem älteren Backend-Commit (`566a09b`, "Benutzertyp als Pflichtfeld") jede Anfrage ohne gültiges `accountType` (`PRIVATE`/`BUSINESS`) mit einem Validierungsfehler ab — iOS' `RegisterRequest` hatte dieses Feld nie bekommen, **Registrierung war auf iOS also komplett funktionsunfähig**. Behoben: `RegisterRequest`/`AuthRepository.register()` um `accountType` ergänzt, neue `AccountTypeSelector`-View (`CommonComponents.swift`, mirrort Androids gleichnamige Composable) in `RegisterView.swift` eingebaut, `RegisterViewModel` validiert vor dem Absenden. (2) **i18n-Bundle war massiv veraltet**: die 6 JSON-Dateien unter `Resources/i18n/` stammten noch aus Phase 13 (2026-09-06) und hatten seither keine der ca. 9 Android-Tranchen an neuen Web-Keys mitbekommen (`fr.json` z. B. 736 statt aktuell 1504 Zeilen) — alle 6 Sprachen 1:1 aus `soukmar/src/assets/i18n/` neu kopiert, behebt rückwirkend jeden `t()`-Aufruf, der bisher nur den rohen Key anzeigte. (3) **Rebrand SoukMar → SouqMar24**: `SoukMarLogo` (`CommonComponents.swift`), `HomeView`s `navigationTitle` und `project.yml`s `CFBundleDisplayName` aktualisiert — mirrort Androids Tranche-7-Rebrand. (4) **7 fehlende Kategorien ergänzt**: `CATEGORIES` in `CatalogModels.swift` hatte nur die ursprünglichen 11 Einträge — `LESSONS_COURSES`, `CARPOOLING`, `TRANSPORT`, `RENTAL`, `TICKETS`, `GIVEAWAY_SWAP`, `MOVING` ergänzt (Werte/Farben/Reihenfolge 1:1 aus Androids aktueller `CatalogModels.kt` übernommen, inkl. `OTHER` ans Ende verschoben wie in Androids Tranche 9), `BABY_KIDS`-Emoji von 🍼 auf 🧸 angeglichen. Da `CATEGORIES` app-weit die einzige Quelle ist (Home-Grid, Anzeige-Assistent, Listings-Filter lesen alle direkt daraus), war keine weitere Änderung an den Views nötig. **Bewusst zurückgestellt** (spätere iOS-Tranchen, mirrort Androids Tranchen 1-9 der Reihe nach): internationaler Telefon-Vorwahl-Picker, Passwort-Auto-Ausblenden, Anzeigen-Ablauf+Verlängern-Button, Chat-Blockieren, SMS-Telefonverifizierung, Rechtstexte, Kartenansicht, MULTI_SELECT/DATE-Attributtypen, Konto-Löschung, gespeicherte Suchen bearbeiten, ähnliche Anzeigen, Bildkomprimierung, zentrale Einstellungen-Seite, Job-Autocomplete, Kontotyp-Badges/-Filter, NO_CONDITION_SUBCATEGORIES, kategoriespezifische Platzhalter, Verifiziert-Badge im Chat, Kategorie-Icons (SVG statt Emoji) — alles bereits auf Android gelöst und dort als Referenz nutzbar. **Codemagic-Build bestätigt grün** (Commit `3279bd6`) — damit gelten rückwirkend auch die zwei zuvor nie bestätigten Phasen 10 (Gespeicherte Suchen) und 13 (i18n) als kompilierbar, da ein Codemagic-Build immer den gesamten aktuellen Codestand kompiliert. Weiterhin nur Kompilier-Check: der Registrierungs-Fix, die neuen Kategorien und der aktualisierte i18n-Bundle-Inhalt sind syntaktisch korrekt und bauen, aber ein echtes Durchklicken (Registrierung mit Kontotyp-Auswahl, neue Kategorien im Grid/Filter/Assistent) steht mangels Simulator/Gerät weiterhin aus.
- **Tranche 2**: (1) **Internationaler Telefon-Vorwahl-Picker**: neue `Models/DialCodes.swift` (1:1-Port von Androids `DialCodes.kt`, 196 Länder als `DialCode`-Struct-Array, `flagEmoji()`/`dialCodeByIso()`/`parsePhone()`/`composePhone()` als freie Funktionen), neue `Views/PhoneInputField.swift` (Flaggen+Vorwahl-Button öffnet ein `.sheet` mit `List` + natives `.searchable()` statt Androids manuell gebauter Such-TextField+LazyColumn-Dialog — idiomatischer in SwiftUI). Ersetzt alle 4 reinen Telefon-`TextField`s: `RegisterView`, `ProfilView`, `DeposerAnnonceView` (Telefon **und** WhatsApp). Kein Änderung an den zugrunde liegenden `@Published var phone/whatsapp: String`-Feldern nötig — sie speichern weiterhin nur einen String, jetzt eben im vorwahl-präfigierten Format (`+212612345678`), exakt wie bei Android; keine Validierungslogik in den ViewModels hätte das Format vorausgesetzt. (2) **Passwort-Auto-Ausblenden**: neue `PasswordField`-View (`CommonComponents.swift`, mirrort Androids `AppTextField(isPassword = true)`) — Augen-Icon zum Ein-/Ausblenden, blendet nach 8s automatisch wieder aus (`Task.sleep` mit einem gespeicherten, bei jedem Sichtbarkeits-Wechsel neu gestarteten/abgebrochenen `Task`-Handle, mirrort Androids `LaunchedEffect(passwordVisible)`-Cancel-und-Neustart-Semantik, die ein reines `Task { }` in `.onChange` ohne Cancel-Handling nicht von selbst hätte). Ersetzt alle 6 `SecureField`s: `LoginView`, `RegisterView` (Passwort + Bestätigung), `ProfilView` (aktuell/neu/bestätigen). **Wichtige Plattform-Falle vermieden**: SwiftUIs neuer zweiparametriger `.onChange(of:) { old, new in }`-Callback ist erst ab iOS 17 verfügbar, dieses Projekt zielt aber auf iOS 16 (`project.yml`s `deploymentTarget`) — durchgängig die ältere einparametrige Form `.onChange(of:) { new in }` verwendet, wie es der Rest der Codebase (`ProfilView`, `ChatView`, `DeposerAnnonceView`) auch schon tut. **Codemagic-Build bestätigt grün** (Commit `254b53e`).
- **Tranche 3**: Beim Scoping entdeckt — `attributeField()`s `switch def.type` im Anzeige-Assistenten (`DeposerAnnonceView.swift`) hatte **keinen Fall für `MULTI_SELECT`/`DATE`** (fiel in den `default`-Zweig, ein reines Text-Freifeld) — dieselbe Lücke, die Android in seiner eigenen Tranche 3 gefunden hatte, hier aber nicht ganz so schwerwiegend, da wenigstens ein Freitextfeld statt gar nichts angezeigt wurde. Trotzdem funktional kaputt: Pflichtattribute dieser Typen (z. B. `SUBJECT` bei Cours & Leçons, `EVENT_DATE` bei Billets & Tickets) ließen sich so nicht korrekt befüllen, das Backend (`z.array(...).min(1)` bzw. `z.iso.date()` in `soukmar-backend/src/lib/attributes.ts`) hätte eine falsch formatierte Freitext-Eingabe abgelehnt. Behoben: MULTI_SELECT als Checkbox-Liste (neue `attrMulti: [String: [String]]`-State + `toggleAttrMulti()` in `DeposerAnnonceViewModel`, neuer `AttrValue.stringArray([String])`-Fall für die Übertragung als JSON-Array), DATE über natives SwiftUI-`DatePicker` (ISO-"yyyy-MM-dd"-Konvertierung via `DateFormatter`, passend zu Backends `z.iso.date()`). Dabei zusätzlich denselben Daten-Bug wie bei Android gefunden und gefixt: `applyListingToForm()`s naive `for av in listing.attributeValues { attrText[code] = ... }`-Schleife überschrieb bei MULTI_SELECT-Attributen (mehrere Zeilen mit demselben Code, eine pro gewählter Option) stillschweigend alle bis auf die letzte gewählte Option beim Laden einer Anzeige zum Bearbeiten — jetzt korrekt per `Dictionary(grouping:)` zusammengeführt. Gleiches Gruppierungsproblem auch auf der Anzeigendetailseite behoben (`ListingDetailView.swift`s `specRow` zeigte vorher eine Zeile pro MULTI_SELECT-Wert statt einer gemeinsamen, komma-getrennten Zeile) — dabei gleich noch eine locale-abhängige Datumsformatierung für DATE-Attribute ergänzt (`DateFormatter` mit `i18n.currentLang` als Locale, mirrort Androids `DateTimeFormatter.ofLocalizedDate(MEDIUM)`-Ansatz), da der rohe ISO-String sonst unformatiert angezeigt worden wäre. **Codemagic-Build bestätigt grün** (Commit `77e3892`).
- **Tranche 4**: **Nutzer im Chat blockieren/entblocken** (mirrort Androids Tranche 2). Backend-Endpunkte existierten bereits fertig (`POST`/`DELETE users/{id}/block`, von Android längst genutzt) — nur die iOS-Seite fehlte komplett. `ConversationDto` um `blockedByMe`/`blockedByThem: Bool` (manueller Decode wie beim Rest des Structs, da Swifts synthetisiertes `Decodable` fehlende Keys mit Property-Defaults nicht toleriert) + berechnete `messagingBlocked`-Property ergänzt; neue `UserRepository.blockUser()`/`unblockUser()`; `ChatViewModel` um `confirmBlock`/`blockSubmitting`-State + `requestBlockToggle()`/`confirmBlockToggle()`/`dismissBlockConfirm()` ergänzt (Blockieren verlangt eine Bestätigung, mirrort Webs `confirm()` bzw. Androids `AlertDialog`; Entblocken läuft sofort ohne Nachfrage). `ChatView.swift`: neuer Toolbar-Button (SF-Symbol `nosign`/`minus.circle` je nach Zustand) neben dem bereits vorhandenen Melden-Flag, `.alert(...)` für die Blockier-Bestätigung, und die komplette Eingabezeile (Angebots-Panel, Schnellantworten, Nachrichtenfeld) wird durch eine Banner-Zeile ersetzt, sobald `messagingBlocked()` wahr ist — exakt Androids `if (blocked) { Banner } else { ... Eingabe ... }`-Struktur. **Codemagic-Build bestätigt grün** (Commit `039df91`).
- **Tranche 5**: **Rechtstexte** (mirrort Androids Tranche 2). Neue `Views/LegalPageView.swift`: generische `LegalPageView` (Titel-Key + i18n-Namespace + Sektionsanzahl → rendert `sN_title`/`sN_body` aus den geteilten i18n-Assets, kein Backend-Zugriff nötig — exakt Androids `LegalPageScreen`-Ansatz), `LegalNoticeView` als Hub-Seite ("Mentions légales" + drei Links zu Datenschutz/Nutzungsbedingungen/Widerrufsrecht, mirrort Androids `extraLinks`-Parameter), neuer `LegalRoute`-Hashable-Wrapper (vierter Konversations-artiger Wrapper nach `String`/`ConversationRoute`/`SellerRoute` für die drei Unterseiten). Einstieg: neuer Menüpunkt "Mentions légales" im `HomeView`-Overflow-Menü (neuer `Route.legalNotice`-Fall). **Bewusst kein WebView-Ansatz** (ursprünglich im allerersten Android-Plan als Option erwähnt) — Android hat sich für native Screens mit geteiltem i18n-Content entschieden, iOS übernimmt das 1:1 statt eine zweite, inkonsistente Lösung zu bauen. **Codemagic-Build bestätigt grün** (Commit `fd2c868`).
- **Tranche 6**: **Konto-Löschung** (mirrort Androids Tranche 4). Backend-Endpunkt existierte bereits (`DELETE auth/account`, anonymisiert Name/E-Mail/Telefon/Bild und schließt aktive Anzeigen) — nur die iOS-Seite fehlte. Neue `AuthRepository.deleteAccount(password:)` — auf Android brauchte das ein `@HTTP(method="DELETE", hasBody=true)`-Workaround, da Retrofits `@DELETE` keinen Body direkt unterstützt; ein rohes `URLRequest` kennt diese Einschränkung gar nicht, daher hier einfach ein normaler `send(method: "DELETE", body:)`-Aufruf ohne Sonderbehandlung. Neue `DeleteAccountViewModel`/`DeleteAccountView`: Warnbox, Passwort-Reeingabe (wiederverwendet die `PasswordField` aus Tranche 2), Pflicht-Bestätigungs-Checkbox, destruktiver Absenden-Button — nach Erfolg `authRepository.logout()` + kurze Verzögerung + `onLoggedOut()`-Callback (derselbe Callback, den auch der bestehende Logout-Menüpunkt nutzt). Einstieg: neuer, rot hervorgehobener Menüpunkt im `HomeView`-Overflow-Menü, direkt über "Se déconnecter". **Codemagic-Build bestätigt grün** (Commit `744b3be`).
- **Tranche 7**: **Gespeicherte Suchen bearbeiten** (mirrort Androids Tranche 4). Backend-Endpunkt existierte bereits (`PATCH saved-searches/{id}`) — nur die iOS-Seite fehlte. Neue `SavedSearchRepository.update(id:_:)`; `ListingsViewModel` um `editSearchId`-State erweitert, `saveSearch()` verzweigt jetzt zwischen `create()`/`update()` je nachdem ob gesetzt, `cancelSaveSearch()` setzt es zurück; neue `applySavedSearchForEdit(_:)` (Pendant zu `applySavedSearchById`, lädt zusätzlich Namen vor und öffnet das Speicher-Formular direkt). `HomeView`s `Route.listings`-Fall um ein drittes `editSearchId`-Assoziierungsfeld erweitert (Swift-Enum-Cases unterstützen anders als Funktionsparameter **keine** Default-Werte für assoziierte Werte — daher an allen 4 bestehenden Konstruktionsstellen explizit `editSearchId: nil` ergänzt statt sich auf einen Default zu verlassen). `SavedSearchesView` bekommt einen neuen "Modifier"-Stift-Button neben dem bestehenden Lösch-Kreuz, neuer `onEditSearch`-Callback. `ListingsView`s Speichern-Button-Label wechselt zu "Mettre à jour", sobald `editSearchId` gesetzt ist. **Codemagic-Build bestätigt grün** (Commit `9a70f40`).
- **Tranche 8**: **Ähnliche Anzeigen** auf der Anzeigendetailseite (mirrort Androids Tranche 4). Backend-Endpunkt existierte bereits (`GET listings/{id}/similar`) — nur die iOS-Seite fehlte. Neue `ListingRepository.getSimilar(id:)`, `ListingDetailViewModel` um `similarListings`-State + `loadSimilar()` erweitert (nicht-essenzieller Abschnitt, scheitert still bei Fehler wie bei Web/Android). `ListingDetailView` bekommt eine neue Sektion "Cela pourrait aussi vous intéresser" — **bewusst als horizontal scrollender Streifen statt Androids Zeilen-Paar-Workaround**: Android musste die verschachtelte Grid-in-Scroll-Höhenproblematik mit manuell gebauten Row-Paaren umgehen, ein horizontal scrollendes `ScrollView` auf iOS hat dieses Problem gar nicht erst, da es keine unbegrenzte Höhe berechnen muss. Wiederverwendet die bestehende `ListingCardView` + den bereits registrierten `navigationDestination(for: String.self)` an `HomeView`s Stack-Wurzel (kein neuer Wrapper-Typ nötig, da Listing-IDs bereits als roher `String` gepusht werden). **Codemagic-Build bestätigt grün** (Commit `dc8498a`).
- **Tranche 9**: **Bildkomprimierung vor dem Upload** (mirrort Androids Tranche 4). Beim Scoping einen echten, bereits bestehenden Bug gefunden: `UploadRepository.uploadImages()` sendete nie ein `type`-Feld — das Backend (`soukmar-backend/src/routes/upload.ts`) fällt ohne dieses Feld immer auf das `listing`-Cloudinary-Preset zurück (1200px-Limit-Crop), sodass **jeder Avatar-Upload auf iOS bisher versehentlich durchs Listing-Preset statt durchs Avatar-Preset (400×400 Face-Crop) lief** — exakt derselbe Bug, den Android in seiner eigenen Tranche 4 fand und fixte. Behoben zusammen mit der eigentlichen Komprimierung: `APIClient.upload()` bekommt einen neuen optionalen `fields: [String: String]`-Parameter für zusätzliche Multipart-Textfelder; `UploadRepository.uploadImages()` bekommt einen `type`-Parameter (Default `"listing"`), skaliert jedes Bild vor dem Senden auf die längere Seite ≤ 1600px (Listing) bzw. ≤ 640px (Avatar) herunter und kodiert es als 82%-JPEG neu (`UIGraphicsImageRenderer` + `UIImage.jpegData`, mirrort Androids `BitmapFactory`/`Bitmap.compress()`-Ansatz — fällt bei nicht dekodierbaren Bytes auf die Originaldaten zurück statt den ganzen Upload scheitern zu lassen). Beide Aufrufstellen aktualisiert: `DeposerAnnonceViewModel` sendet jetzt explizit `type: "listing"`, `ProfilViewModel`s Avatar-Upload `type: "avatar"`. **Codemagic-Build bestätigt grün** (Commit `e8ef4be`).
- **Tranche 10**: **Verifiziert-Badge** (mirrort Androids Tranche 1 + Tranche 6 zusammen — auf iOS gab es das Badge bisher an **keiner** Stelle, während Android es zunächst breit einführte und später noch um den Chat ergänzte; hier gleich vollständig umgesetzt statt zwei separate Tranchen). Neue `VerifiedBadge`-View (`CommonComponents.swift`, mirrort Webs `app-verified-badge`/Androids gleichnamige Composable: rendert nichts, wenn keines der beiden Flags gesetzt ist). `emailVerified`/`phoneVerified` (jeweils mit manuellem Decoder, da Swifts synthetisiertes `Decodable` fehlende Keys nicht mit dem Property-Default aushilft) ergänzt zu `ChatUserDto`, `ListingUserDto`, `SellerProfileDto`; neue `ConversationDto.partnerUser(myId:)`-Methode (Pendant zu `partnerId()`/`partnerName()`). Eingebunden an 4 Stellen: `ChatListView`s Konversationszeile, `ChatView`s Header (dafür `.navigationTitle` durch ein `ToolbarItem(placement: .principal)` mit Name+Badge übereinander ersetzt, da eine reine String-`navigationTitle` keinen Platz für eine zweite Zeile bietet), `ListingDetailView`s Verkäuferkarte, `SellerProfileView`s Profilkopf. **Codemagic-Build bestätigt grün** (Commit `0531d6b`).
- **Tranche 11**: **Kontotyp (Privat/Gewerblich) vervollständigt** (mirrort Androids Tranche 5). `accountType` war seit Tranche 1 nur bei der Registrierung erfasst worden — jetzt im eigenen Profil bearbeitbar (`ProfilViewModel`/`ProfilView`, wiederverwendet die aus der Registrierung bekannte `AccountTypeSelector`), als Badge auf Verkäuferprofil + Anzeigendetail-Verkäuferkarte sichtbar (neue `AccountTypeLabel`-View, mirrort Androids gleichnamige Composable) und als immer sichtbarer Filter in der Anzeigensuche (`ListingsViewModel.selectedAccountType`, Top-Level-Query-Param `accountType`, kein `attr_`-Präfix — wird bewusst **nicht** beim Kategorie-/Subkategoriewechsel zurückgesetzt, nur bei `clearFilters()`, exakt wie bei Android). `accountType: String?` (mit manuellem Decoder) ergänzt zu `UserDto`, `ListingUserDto`, `SellerProfileDto`; `AuthRepository.updateProfile()` bekommt einen neuen optionalen `accountType`-Parameter. **Codemagic-Build bestätigt grün** (Commit `6824251`).
- **Tranche 12**: **NO_CONDITION_SUBCATEGORIES + kategoriespezifische Platzhalter** (mirrort Androids Tranche 6, dort ebenfalls gebündelt). Neue `NO_CONDITION_SUBCATEGORIES`-Konstante (`CatalogModels.swift`, aktuell nur `TRAINING_OFFERS`) für Subkategorien, die trotz physischer-Güter-Kategorie kein Neuf/Occasion-Feld zeigen sollen. `DeposerAnnonceViewModel.showCondition` berücksichtigt jetzt zusätzlich die gewählte Subkategorie (vorher reine Kategorie-Prüfung); neue, analoge `ListingsViewModel.showCondition`-Property ersetzt die bisherige reine Kategorie-Prüfung in `ListingsView.swift`s Gate für die Zustands-Chips in der Suche — beide Stellen hatten unabhängig voneinander dieselbe Lücke wie ursprünglich bei Android. Kategoriespezifische Platzhalter: Titel-/Beschreibungsfeld im Anzeige-Assistenten zeigen jetzt `deposer.placeholder_title_<category>`/`placeholder_desc_<category>` als Platzhaltertext statt der generischen Variante — alle 18 Keys waren durch den i18n-Resync in Tranche 1 bereits vorhanden, reine UI-Verdrahtung. **Codemagic-Build bestätigt grün** (Commit `0d80872`).
- **Tranche 13**: **Chat-Zeitstempel links** (letzter fehlender Teil aus Androids Tranche 7 — Rebrand und Kategorie-Reihenfolge aus derselben Android-Tranche waren bereits in iOS-Tranche 1 mit erledigt worden, geprüft und bestätigt vor dieser Tranche). `ChatView.swift`: neue `msgTimeLabel(_:)`-Hilfsview (Datum über Uhrzeit, zweizeilig) ersetzt die bisherige einzeilige `Text(formatMsgTime(...))` über der Bubble; `textBubble`/`offerBubble` von `VStack` auf `HStack(alignment: .bottom)` mit dem Zeit-Label links neben der Bubble umgestellt (mirrort Androids `MsgTimeLabel`+`Row`-Umbau exakt), Bubble-Maximalbreite von 280/260 auf 260 reduziert, um Platz für das Label zu schaffen. `formatMsgTime(_:) -> String` zu `formatMsgTimeParts(_:) -> (String, String)` erweitert (liefert jetzt `dd.MM.` und `HH:mm` getrennt statt nur `HH:mm`). **Ausstehend**: Androids Tranche 8 (Standort-/Angebot-Icons professionalisieren, Maschinentyp-Emoji) und Tranche 9 (Kategorie-Icons Emoji→Vektor) sind auf iOS noch nicht portiert. **Codemagic-Build bestätigt grün** (Commit `9966aee`, deckt auch Tranche 14 mit ab, da beide zusammen in diesem Commit committed wurden).
- **Tranche 14**: **Job-Berufsvorschläge-Autocomplete** (zweite Hälfte von Androids Tranche 5 — die erste Hälfte, Kontotyp-Vervollständigung, war bereits in iOS-Tranche 11 erledigt). Neue `Views/TextAutocompleteField.swift` (mirrort Androids gleichnamige Composable/Webs `TextAutocompleteComponent`: Freitextfeld mit gefilterten Vorschlägen, getippter Wert bleibt immer frei editierbar, Auswahl eines Vorschlags schreibt dessen übersetztes Label ins Feld statt eines Codes, diakritik-/groß-kleinschreibungsunabhängiger Filter via `.folding(options: .diacriticInsensitive)`). `CatalogModels.swift` um `JOB_PROFESSIONS_BY_SECTOR` (18 Branchen, 1:1 aus `listing.model.ts`/Androids `DialCodes`-Pendant übernommen) + abgeleitetes, dedupliziertes `JOB_PROFESSION_CODES` ergänzt. `DeposerAnnonceView.swift`s `attributeField(_:)` behandelt den Attribut-Code `PROFESSION` jetzt als Sonderfall (filtert Vorschläge nach dem bereits gewählten `INDUSTRY`-Attributwert, sonst die volle Liste), alle anderen Typen laufen weiterhin durch die umbenannte `attributeFieldByType(_:)`. i18n-Namespace `job_professions.*` war durch den in Tranche 1 kopierten Web-JSON-Bundle bereits vorhanden (gegen Android per `diff` bestätigt: `fr.json` beider Apps sind byte-identisch, 1504 Zeilen) — keine i18n-Änderung nötig. **Hinweis**: Diese Tranche lag bereits vollständig geschrieben, aber uncommitted im Arbeitsverzeichnis vor (aus einer vorherigen, unterbrochenen Session) — beim Fortsetzen gefunden, gegen Android gegengeprüft (Feature-Parität + Code-Qualität bestätigt) und zusammen mit Tranche 13 committed, statt sie zu verwerfen.
- **Tranche 15**: Die beiden in Tranche 13 als ausstehend notierten Punkte (Androids Tranche 8+9) nachgezogen. (1) **Standort-/Angebot-Icons**: 📍 (Standort) → SF-Symbol `mappin` und 💰 (Angebot) → SF-Symbol `tag`, überall wo Android/Web sie durch echte Icons ersetzt haben — `ListingDetailView.swift` (Titel-Meta-Zeile + Verkäuferkarte), `SellerProfileView.swift`, `SavedSearchesView.swift` (dort musste `meta` von `[String]` auf `[Text]` umgestellt werden, da SwiftUI `Text(Image(systemName:))`-Konkatenation braucht, um ein SF-Symbol in eine mit " · " verkettete Zeile einzubetten), `MesAnnoncesView.swift`, `ChatView.swift` (Angebots-Button, -Panel, -Bubble-Label). `private`/`business`/`report` waren auf iOS von Anfang an SF-Symbole (`person`/`building.2`/`flag`, s. `AccountTypeLabel` aus Tranche 11) — anders als Android brauchte iOS hier keinen Nacharbeits-Schritt. Die einzige rein dekorative Emoji-Stelle (Bild-Platzhalter-Overlay bei fehlendem Foto) blieb bewusst unverändert, exakt wie bei Android/Web. (2) **Kategorie-Icons Emoji→Vektor**: neue `Models/CategoryIcons.swift` mit einem selbstgeschriebenen SVG-Pfad-Mini-Parser (`SVGPath.parse(_:) -> Path`, unterstützt M/L/H/V/C/S/A/Z inkl. impliziter Befehlswiederholung und mehrerer Subpfade in einem `d`-String — Kreisbögen werden über die W3C-Endpoint-to-Center-Parametrisierung in kubische Bézier-Segmente umgerechnet, da `Path` keinen nativen SVG-Arc-Befehl kennt) als Pendant zu Androids `PathParser`, da SwiftUI anders als Compose keinen eingebauten SVG-Pfad-Parser mitbringt. `CategoryIcon`-View zeichnet alle 18 Icons per `Canvas` (skaliert auf die 24×24-viewBox wie Web/Android), `d`-Strings 1:1 aus `cat-icon.component.html` übernommen, `<rect>`/`<circle>`/`<line>`/`<polyline>` über native `Path`-Grundformen, CARPOOLINGs gestrichelte Mittellinie über `StrokeStyle(dash:)`. Eingebunden an genau den 3 Stellen, die auch Android/Web umgestellt haben: `HomeView.swift` (Kategorienraster — iOS hat keine "Vos centres d'intérêts"-Sektion, daher hier nur 1 statt Androids 2 Einbindungsstellen), `ListingsView.swift` (Kategorie-Filter-Chips — `ChipView` bekam dafür einen `category`-Parameter statt des bisherigen `emoji`-Parameters), `DeposerAnnonceView.swift` (Kategorie-Auswahlschritt, Tint wechselt bei Auswahl auf `Primary` wie bei Android). Listing-Card-Badge, Anzeigendetail-Badge, Mes-Annonces und gespeicherte Suchen behalten bewusst `CategoryConfig.emoji`, exakt wie bei Android/Web unverändert gelassen. **Ausstehend**: kein lokaler Compiler/Simulator verfügbar — insbesondere der neue Bézier-Arc-Code ist bisher nur durch sorgfältiges Gegenlesen, nicht durch tatsächliches Rendering verifiziert; falls ein Icon nach dem Codemagic-Build sichtbar verzerrt wirkt, zuerst dort ansetzen. **Codemagic-Build bestätigt grün** (Commit `15ee21b`).
- **Tranche 16**: **SMS-Telefonverifizierung im Profil** (Androids Tranche 2, letzter noch offener Punkt aus der ursprünglichen Rückstands-Liste außer der bewusst zurückgestellten Kartenansicht). Backend-Endpunkte (`POST /auth/phone/send-code`, `POST /auth/phone/verify` mit `{code}`) existierten bereits vollständig (10-Minuten-Ablauf, 5-Versuche-Limit, Rate-Limiter) — nur die iOS-Seite fehlte. `AuthRepository.swift` um `sendPhoneCode()`/`verifyPhoneCode(_:)` ergänzt, neue `PhoneVerifyRequest` in `AuthModels.swift`. **Nebenbei eine Lücke gefunden**: `UserDto` (das eigene-Profil-Modell) hatte bisher gar kein `emailVerified`/`phoneVerified`-Feld — anders als `ChatUserDto`/`ListingUserDto`/`SellerProfileDto`, die das seit Tranche 10 haben. Ergänzt (mit demselben `decodeIfPresent(...) ?? false`-Handling wie überall sonst) plus einem expliziten Memberwise-`init(...)`, da der synthetisierte Initializer durch den vorhandenen `init(from:)` unterdrückt wird und `verifyPhoneCode()` lokal ein aktualisiertes `UserDto` bauen muss (Backends `PUT /auth/profile`-Antwort enthält kein `emailVerified`-Feld — unkritisch, da die eigene Profilseite nie ein E-Mail-Verified-Badge zeigt, nur `phoneVerified`). Neue `PhoneVerificationRow`-View (`ProfilView.swift`, private) mirrort Androids `PhoneVerificationRow`-Composable 1:1: drei Zustände (verifiziert / Code anfordern / Code eingeben+bestätigen+erneut senden), nur sichtbar wenn ein Telefonwert gesetzt ist; `ProfilViewModel.swift` um die passenden `@Published`-States + `sendPhoneCode()`/`verifyPhoneCode()` ergänzt, inkl. Reset des Verifizierungs-Zustands beim Ändern der Telefonnummer in `saveProfile()` (mirrort Androids identischen Reset). **Codemagic-Build bestätigt grün** (Commit `a51bf2e`).
- **Tranche 17**: **Zentrale Einstellungen-Seite** (Androids Tranche 4, letzter noch offener Punkt aus dem ursprünglichen Rückstand außer der bewusst zurückgestellten Kartenansicht — damit ist der komplette Android-Rückstand bis Tranche 4 sowie alle seither entstandenen Web-Tranchen bis 9 auf iOS nachgezogen). Neue `SettingsView.swift` bündelt Profil, Notifications, Rechtliches und Konto-löschen (plus Logout-Button) in einer Liste mit Icon+Titel+Subtitel-Zeilen, exakt wie Webs `/parametres`-Hub und Androids `SettingsScreen` — Premium/Aide bewusst ausgelassen (Marketing-/Zahlungsinhalte, kein App-Pendant, identische Begründung wie bei Android). `HomeView.swift`s Overflow-Menü (`⋮`) dafür entschlackt: Profil/Rechtliches/Konto-löschen/Logout raus (jetzt nur noch über Settings erreichbar), neuer `Route.settings`-Eintrag rein — übrig bleiben nur noch Gespeicherte Suchen, Admin (falls Rolle) und der neue Settings-Eintrag, mirrort exakt Androids `DropdownMenu`-Reduktion in `HomeScreen.kt`. **Codemagic-Build bestätigt grün** (Commit `629fa4e`).
- **Tranche 18**: **Kartenansicht** — bisher auf beiden Plattformen bewusst zurückgestellt wegen der irrtümlichen Annahme, dafür sei ein bezahlter Google-Maps-API-Key nötig. Beim Scoping festgestellt: Web nutzt Leaflet mit kostenlosen OpenStreetMap-Kacheln (`listings-map.component.ts`), und Backend geokodiert `lat`/`lng` serverseitig beim Anlegen einer Anzeige über die ebenfalls kostenlose Nominatim-API (`soukmar-backend/src/lib/geocode.ts`, city-Centroid-Genauigkeit, gecacht) — der Client bekommt `lat`/`lng` einfach als fertige Felder auf jedem `ListingDto` mitgeliefert, keinerlei Geocoding- oder Kartenlizenz-Aufwand auf App-Seite nötig. Für iOS bedeutet das: natives **MapKit** (kein Key, kein SDK-Setup, seit iOS 14 in SwiftUI eingebaut) statt eines Leaflet-Äquivalents. `ListingDto` um `lat`/`lng: Double?` ergänzt (fehlte bisher komplett, anders als Android, das die Felder schon aus einer früheren Mirror-Runde hatte, ohne sie zu nutzen). Neue `ListingsMapView.swift`: `Map(coordinateRegion:annotationItems:)` mit einem Pin pro Anzeige (nur die mit gesetztem `lat`/`lng`), automatischer Zoom-auf-alle-Pins via eigener Bounding-Box-Berechnung (Pendant zu Leaflets `fitBounds`), Pin-Tap navigiert über `NavigationLink(value:)` zur Anzeige (nutzt den bereits an `HomeView`s Stack-Wurzel registrierten `String.self`-Destination, kein neuer Navigationspfad nötig). Eingebunden an genau den 2 Stellen wie im Web: `ListingsView.swift` (neuer Liste/Karte-Umschalter über den Ergebnissen, nur sichtbar wenn Ergebnisse vorhanden sind) und `ListingDetailView.swift` (neue, standardmäßig eingeklappte `locationSection` zwischen Spezifikationen und Kontakt-Karte, exakt wie Webs `detail__location`-Card — wiederverwendet dieselbe `ListingsMapView` mit einem Einzelelement-Array, genau wie Web dieselbe `app-listings-map`-Komponente an beiden Stellen wiederverwendet). **Bewusst nicht Teil dieser Tranche**: Websn "Meinen Standort verwenden"-Umkreissuche (Browser-Geolocation → `lat`/`lng`/`radius`-Query-Parameter → Distanz-Sortierung) — eigenständiges, größeres Feature mit `CoreLocation`-Berechtigung (`NSLocationWhenInUseUsageDescription` in der XcodeGen-`project.yml`), separat zu scopen falls gewünscht. **Codemagic-Build bestätigt grün** (Commit `3f3e24f`).
- **Tranche 19**: Die in Tranche 18 zurückgestellte **"Meinen Standort verwenden"-Umkreissuche + Distanz-Sortierung** nachgezogen. Backend-Vertrag bereits vollständig vorhanden (`GET /listings?lat&lng&radius&tri=distance` — Haversine-Umkreis-Query + serverseitige Distanz-Sortierung, siehe `soukmar-backend/src/routes/listings.ts`); reine Client-Ergänzung. Neue `Networking/LocationService.swift`: schlanker `CLLocationManagerDelegate`-Wrapper, der die klassische Delegate-Callback-API in ein einziges `async` `requestCurrentLocation()` übersetzt (Pendant zu Webs `navigator.geolocation.getCurrentPosition()`) — als `@MainActor`-Klasse mit `nonisolated`-Delegate-Methoden, da `CLLocationManagerDelegate` ein nicht-isoliertes (Objective-C-) Protokoll ist. `project.yml` um `NSLocationWhenInUseUsageDescription` ergänzt (bisher fehlte jede Info.plist-Berechtigung für Standortzugriff). `ListingsViewModel.swift`: neue `lat`/`lng`/`radius`/`sortBy`-States, `useCurrentLocation()`/`clearLocation()`/`setRadius()`/`setSort()`, `buildParams()` um `lat`/`lng`/`radius`/`tri` erweitert, `clearFilters()` setzt auch diese zurück (mirrort Webs `resetFilters()`). **Dabei auffällig**: iOS (wie Android) hatte bisher überhaupt **keine Sortierung** auf der Anzeigen-Suche — dieser Baustein war schon vor dieser Tranche eine eigenständige Lücke, jetzt im selben Zug mit erledigt (Neueste/Preis auf-/absteigend/Distanz). Neue Sektionen in `ListingsView.swift`s `FiltersSheet`: "Standort" (GPS-Button → nach Erfolg Radius-Picker 5–200 km + Löschen-Button, Fehleranzeige bei verweigerter Berechtigung) und "Sortieren" (Distanz-Option nur sichtbar wenn ein Standort aktiv ist, exakt wie Webs `@if (filters.lat)`-Gate). Kein Pendant zu Webs freier Stadt-Textsuche gebaut (existierte auf keiner der beiden nativen Apps je, bewusst außerhalb dieses Scopes belassen — reiner GPS/Radius/Distanz-Fokus, wie vom Nutzer angefragt). **Codemagic-Build bestätigt grün** (Commit `790c3fc`).
- **Tranche 20 (2026-09-19): Internationalisierung (globale Länderauswahl)** — zieht Webs Feature UND Androids Tranchen 11+12 (bereits im Emulator end-to-end verifiziert) in einem Zug nach, statt wie Android in zwei Tranchen: das Land, in dem eine Anzeige steht/gesucht wird, ist nicht mehr hart Marokko, UND von Anfang an zentral über einen `CountrySwitcher` gesteuert (kein separates, unabhängig änderbares Formularfeld — Web/Android mussten das in einer Nachbesserungsrunde selbst noch korrigieren).
  - **Datenschicht** (`Models/CountryModels.swift`, neu): `COUNTRIES` (193 Länder), `COUNTRY_BY_CODE`, `CountryRegionGroup`/`COUNTRY_REGIONS`, `currencyForCountry()`/`isKnownCountry()`/`regionLabelKey()`, generiert über ein neues Node-Einmalskript (`gen_swift.js`, mirrort Androids `gen_kotlin.js`-Ansatz: aus derselben `countries_data.json`-Zwischendatei wie Android, nicht erneut aus dem Web-TS geparst) — Single Source of Truth bleibt `soukmar/src/app/models/country.model.ts`. **Design-Entscheidung ohne lokalen Compiler**: `COUNTRY_REGIONS` wurde bewusst als `[CountryRegionGroup]` (eigener `Hashable`-Struct mit `region`/`codes`) statt als Tupel-Array gebaut, da unklar war, ob `ForEach(_, id: \.region)` über ein Tupel-Array zuverlässig kompiliert — ein Struct ist eindeutig und ohne Compiler-Rückmeldung die sicherere Wahl; dafür musste `ContinentRegion` zusätzlich `Hashable` deklarieren (raw-value-Enums bekommen das nicht automatisch). `CatalogModels.swift` bekam `MOROCCO_CITIES` (315 Einträge) + einen generalisierten `formatPriceParts(price:currency:lang:)` (via `NumberFormatter(.currency)`, Betrag/Symbol-Trennung durch Herausschneiden von `formatter.currencySymbol` aus dem formatierten String, da Foundation anders als `Intl.NumberFormat.formatToParts()`/Javas `formatToCharacterIterator()` keine strukturierte Teile-API hat). **Zwei Extraktions-Bugs unterwegs gefunden**: (1) das erste `MOROCCO_CITIES`-Extraktionsskript (`extract_morocco.js`, direkt aus `listing.model.ts`) verschluckte escapte Apostrophe (`'Kelaa M\'Gouna'` → captured mit stehengebliebenem Backslash), da dieses Array bewusst einfach- und doppelt-gequotete Strings mischt, um Apostrophe zu escapen statt die Quote-Art zu wechseln — anders als Androids ursprünglicher, längst gefixter "falscher Quote-Stil"-Bug. Statt die Regex zu reparieren, wurde `MOROCCO_CITIES` stattdessen direkt aus Androids bereits korrektem, bereits verifiziertem `CatalogModels.kt` extrahiert (315 `.distinct().sorted()`-Einträge, per `grep` bestätigt: dort schon korrektes `"Kelaa M'Gouna"` ohne Backslash) — Bug dadurch vermieden statt nachträglich gefixt. (2) keiner in `CountryModels.swift` selbst, aber die naheliegende erste Fassung von `COUNTRY_REGIONS` als Tupel-Array wurde präventiv verworfen (s.o.), bevor daraus ein `ForEach`-Kompilierfehler hätte werden können.
  - **`Networking/CountryRepository.swift`** (neu) mirrort `I18nRepository`: `ObservableObject`-Singleton, `@Published private(set) var country`, Default `"MA"`, persistiert in `UserDefaults` (Key `soukmar_country`, unabhängig vom Keychain-Token — übersteht Logout, wie bei Sprache). Erst-Start-IP-Geolocation (`https://ipapi.co/json/`) über einen **bewusst nackten `URLSession(configuration: .ephemeral)`**, nicht `APIClient` (das hängt sonst automatisch den eigenen API-Bearer-Token an jede Anfrage — darf niemals an einen Drittanbieter gehen), mirrort Androids identische Begründung für dessen eigenen nackten `OkHttpClient`.
  - **`Views/CountrySwitcher.swift`** (neu): Flagge+ISO-Code-Button (kompakt wie `LanguageSwitcher`s `🇫🇷 FR`) öffnet ein nach Kontinent gruppiertes, `.searchable()`-durchsuchbares `List`-Sheet mit vollen, lokalisierten Ländernamen (`Locale.localizedString(forRegionCode:)`, JVM-/Web-Äquivalent von `getDisplayCountry()`/`Intl.DisplayNames`). **Bewusst parametrisiert** (`country: String? = nil`, `onSelect: ((String) -> Void)? = nil`, fallen auf den globalen `CountryRepository.shared` zurück, wenn `nil`) statt wie in der allerersten Fassung fest an `CountryRepository.shared` verdrahtet — mirrort Androids `CountrySwitcher(country, onSelect)`-Signatur und erlaubt Wiederverwendung mit einem Screen-lokalen ViewModel-State (`ListingsView`s Filter-Sheet, s.u.), ohne den globalen Zustand zu verändern. **Wichtige Falle dabei vermieden**: ein Default-Parameterwert wie `var country: String = CountryRepository.shared.country` wäre bei jedem Struct-Init einmalig eingefroren und hätte bei einer Länderänderung an anderer Stelle NICHT automatisch neu gerendert (SwiftUI recomputed nur `@ObservedObject`/`@Published`-getriebene Werte, nicht gewöhnliche gespeicherte Properties) — stattdessen `country: String?` (optional, Default `nil`) + eine berechnete `effectiveCountry`-Property, die bei `nil` auf das intern per `@ObservedObject` gehaltene `countryRepository.country` zurückfällt, bleibt für den Default-Aufruf (`CountrySwitcher()` auf Home/Login) voll reaktiv. Eingebunden auf `LoginView.swift` (neben `LanguageSwitcher`) und `HomeView.swift`s Toolbar (vor `LanguageSwitcher`) mit den Defaults, sowie in `ListingsView.swift`s `FiltersSheet` explizit mit `viewModel.country`/`{ viewModel.selectCountry($0) }`.
  - **`DeposerAnnonceViewModel.swift`/`DeposerAnnonceView.swift`**: `country` ist von Anfang an **nicht** im Formular wählbar (iOS hatte nie ein `CountryDropdown`-Äquivalent wie Web/Android es zunächst bauten und in einer Nachbesserungsrunde wieder entfernen mussten) — `@Published private(set) var country` bleibt für neue Anzeigen live mit `CountryRepository.shared.$country` synchron (`Combine`-`.sink` in `start(editId:)`, Combine-Äquivalent von Androids `snapshotFlow`/Webs `effect()` — `@Published`s projizierter Publisher liefert den aktuellen Wert sofort beim Abonnieren, deckt Erstwert UND jede spätere Änderung mit demselben `.sink` ab), ein `countrySyncStarted`-Flag verhindert mehrfaches Abonnieren bei wiederholten `start()`-Aufrufen. Eine bearbeitete Anzeige übernimmt stattdessen ihr gespeichertes `listing.country` (`applyListingToForm`) und wird nie vom Sync überschrieben. `derivedCurrency`/`citiesForCountry` berechnet (kein manuelles Setzen), Preis-Label zeigt `"Prix (\(derivedCurrency))"`. Stadtfeld generalisiert: `CityPickerButton`+`CityPickerSheet` (neu, mirrort `CountryPickerSheet`s Muster) bei kuratierten Ländern, reines `TextField` bei den ~165 nicht kuratierten (leere `citiesForCountry`). `ListingUpsertRequest` verliert `currency` komplett (Backend leitet es serverseitig aus `country` ab), bekommt stattdessen ein Pflicht-`country`-Feld ohne Default (Aufrufer muss es immer explizit mitgeben). `ListingDto`/`ListingUpsertModels.swift` um `country` ergänzt (manueller Decode wie überall, da Swifts synthetisiertes `Decodable` fehlende Keys nicht mit Property-Defaults toleriert).
  - **`ListingsViewModel.swift`**: `country` als reiner **einmaliger Lesezugriff** bei Erstellung (`@Published private(set) var country: String = CountryRepository.shared.country`) plus expliziter `selectCountry(_:)`-Methode (aktualisiert lokal + `CountryRepository.shared.setCountry(_:)` + `search()`) — **kein** Combine-Live-Abonnement wie in `DeposerAnnonceViewModel`. Bewusst unterschiedlich behandelt: ein erster Entwurf baute versehentlich dieselbe `AnyCancellable`/`.sink`-Subscription wie beim Formular-ViewModel, wurde aber vor dem Fertigstellen anhand von Androids tatsächlichem `ListingsViewModel.kt` (`grep`-verifiziert: `var country by mutableStateOf(countryRepository.country)`, keine Flow-Subscription) korrigiert — diese ViewModel-Instanz lebt nur so lange der Screen offen ist, ein Länderwechsel während des Browsens läuft explizit über die neue `CountrySwitcher`-Zeile im Filter-Sheet, nicht implizit von außen. `buildParams()` hängt `country` jetzt unbedingt an (kein optionaler Filter, mirrort Androids Platzierung direkt neben `page`/`limit`).
  - **Ausländer-Flagge** neben der Stadt (mirrort Webs/Androids `foreignFlag`, nur für Nicht-Marokko-Anzeigen): `ListingCardView.swift`/`ListingDetailView.swift` zeigen `flagEmoji(listing.country)`, wenn `listing.country != "MA"`.
  - **i18n-Bundle massiv nachsynchronisiert**: `Resources/i18n/*.json` waren noch auf dem Tranche-1-Stand (1504 Zeilen `fr.json`) eingefroren — alle 6 Sprachen 1:1 aus `soukmar/src/assets/i18n/` neu kopiert (jetzt 1568 Zeilen, u. a. `deposer.label_country`, `deposer.region_asia/north_america/south_america/oceania`, geänderter `nav.topbar_tagline`, entfernter `home.hero_subtitle`), behebt rückwirkend jeden `t()`-Aufruf für zwischenzeitlich neue Web-Keys, nicht nur die dieser Tranche.
  - **Codemagic-Build bestätigt grün** (Commit `27584d2`) — damit ist der komplette Country-/Currency-Code (`CountryRegionGroup`/`ForEach`, beide `Combine`-`.sink`-Stellen, die manuellen `Decodable`-Inits der neuen Felder) bestätigt kompilierbar. Weiterhin nur Kompilier-Check: ein echtes Durchklicken (Länderwechsel während offenem Anzeige-Assistenten, Filter-Sheet-Suche nach Land) steht mangels Simulator/Gerät weiterhin aus.

- **Tranche 21 (2026-09-20): Foto per Kamera aufnehmen** — mirrort denselben Zug auf Web (`<input capture="environment">`) und Android (`ActivityResultContracts.TakePicture()` + `FileProvider`). Bisher konnte man im Anzeige-Assistenten Fotos nur über `PhotosPicker` (iOS 16+) aus der Bibliothek wählen — der hat aber keinen Kamera-Modus, dafür braucht es das ältere `UIImagePickerController(sourceType: .camera)`. Neue `Views/CameraPicker.swift`: `UIViewControllerRepresentable`-Wrapper um `UIImagePickerController`, `Coordinator` als `UIImagePickerControllerDelegate` liefert das aufgenommene Bild als JPEG-`Data` (`compressionQuality: 0.9`) über einen `onCapture`-Closure zurück und schließt sich danach selbst (`@Environment(\.dismiss)`). `project.yml` um `NSCameraUsageDescription` ergänzt (bisher fehlte jede Info.plist-Berechtigung für Kamerazugriff, analog zu `NSLocationWhenInUseUsageDescription` aus Tranche 19). `DeposerAnnonceView.swift`: neuer `@State private var cameraOpen`-Flag + `.fullScreenCover(isPresented: $cameraOpen) { CameraPicker { data in viewModel.addPhotos([PhotoItem(localData: data)]) } }` (Vollbild statt `.sheet`, da die System-Kamera-UI für Vollbild-Präsentation ausgelegt ist), neue Kamera-Kachel im Foto-Grid direkt neben der bestehenden `PhotosPicker`-"Ajouter"-Kachel, nur sichtbar wenn `UIImagePickerController.isSourceTypeAvailable(.camera)` UND das `maxPhotos`-Limit noch nicht erreicht ist. Nutzt dieselbe, bereits generische `viewModel.addPhotos([PhotoItem])`-Methode wie der Bibliotheks-Picker, keine ViewModel-Änderung nötig. Neuer i18n-Key `deposer.take_photo` als Kachel-Beschriftung (aus dem Web-JSON-Bundle übernommen, alle 6 Sprachen, i18n-Bundle im selben Zug erneut resynchronisiert). **Codemagic-Build bestätigt grün** (Commit `5b60039`) — `CameraPicker.swift`, die `NSCameraUsageDescription`-Ergänzung und die neue Kamera-Kachel sind damit bestätigt kompilierbar. Weiterhin nur Kompilier-Check: ein echtes Durchklicken (Kamera öffnen, aufnehmen, Foto erscheint im Grid) steht mangels Simulator/Gerät weiterhin aus.
- **Tranche 22 (2026-09-20): Icon-Professionalisierung** — App-weiter Audit (mirrort denselben Zug auf Web und Android) fand rohe Emoji als funktionale UI-Icons (Buttons, Status-Badges, Verifiziert-Häkchen), wo bereits SF Symbols das etablierte Icon-System dieser App sind — bewusst NICHT angetastet: Kategorie-Emoji, Länderflaggen, `★`-Bewertungsglyphen, `MACHINE_TYPE_ICONS`, große dekorative Empty-State-Icons (40pt, z. B. `AdminView`/`FavorisView`/`ChatListView`/`MesAnnoncesView`/`NotificationsView`/`SavedSearchesView`) und die i18n-String-eigenen Emoji-Präfixe (z. B. "📸 Ajouter des photos") — all das bleibt exakt wie auf Web/Android dokumentiert bewusst bestehen. Ersetzt durch SF Symbols: `CommonComponents.swift`s geteilte `VerifiedBadge`-Pille (✓→`checkmark.seal.fill`) und `SuccessBanner` (✅→`checkmark.circle.fill`, betrifft ~8 Screens auf einen Schlag), `ProfilView.swift`s Telefon-verifiziert-Zeile (dieselbe Siegel-Optik), `ListingCardView.swift`/`ListingDetailView.swift`s Premium-Badge (⭐→`star.fill`), Kontakt-Karte (📞→`phone.fill`, 💬→`message.fill` für WhatsApp/Chat-Button), Preis-Trend-Indikator (📉/📈→`arrow.down/up.right.circle.fill`), Bild-Platzhalter-Fallback bei unbekannter Kategorie (📦→`shippingbox`, an allen 3 Stellen: `ListingCardView`/`ListingDetailView`/`MesAnnoncesView`), `MesAnnoncesView.swift`s Views/Zeit-Metadaten (👁/🕐→`eye`/`clock`), `ChatView.swift`s Reserviert-/Blockiert-Banner + komplettes Angebots-Status-System (ausstehend/akzeptiert/abgelehnt/storniert + Akzeptieren/Ablehnen/Stornieren-Buttons — vorher der dichteste Emoji-Cluster der ganzen App, jetzt `lock.fill`/`nosign`/`clock.fill`/`checkmark.circle.fill`/`xmark.circle.fill`), `ListingsView.swift`s Suche-speichern-Button+Bestätigung (🔔→`bell`, ✅→`checkmark.circle.fill`), sowie das wiederkehrende "📌 Anzeigentitel als Link"-Muster in `AdminView`/`NotificationsView`/`SellerProfileView` (→`link`, dabei auch `AdminView`s Notiz-Präfix 📝→`note.text` und `SellerProfileView`s Antwortzeit-Badge ⚡→`bolt.fill` im selben Zug mitgezogen). Durchgängig SwiftUIs natives `Label(_:systemImage:)` verwendet statt manuell zusammengesetzter `HStack(Image, Text)`-Paare, wo eine einzelne Zeile genügte. **Kein lokaler Compiler verfügbar** — alle Änderungen sorgfältig gegen den vollständigen Diff gegengelesen (korrekte `Label`/`Group`-Verschachtelung, keine unbalancierten Klammern), folgen aber exakt demselben Muster, das an anderer Stelle in dieser Codebase (`AccountTypeLabel`, `ListingsView`s Filter-Icons) bereits verifiziert kompiliert — Codemagic-Build nach diesem Commit muss trotzdem grün bestätigt werden, bevor das als fertig gilt.

- **Tranche 23 (2026-09-21): 5 neue Gratis-Features von Web nachgezogen (Preisalarm, Verkaufs-Trichter, ID-Verifizierung, Bildersuche, maskierte In-App-Anrufe)** — Web (`soukmar`) hat 5 neue kostenlose Features geshippt (Backend fertig in `soukmar-backend`, Web-Frontend fertig und im Browser getestet); Android holt dieselben 5 parallel in seiner eigenen Tranche 15 nach (unabhängig voneinander gearbeitet, kein gegenseitiges Warten). **Wichtige Randnotiz zu dieser Tranche**: in dieser Session war `gh` nicht bei GitHub eingeloggt (kein gespeichertes Token) — Codemagic-Build-Status konnte deshalb dieses eine Mal **nicht** automatisiert abgefragt werden, anders als in den 22 Tranchen davor; der Nutzer hat stattdessen zwischendurch manuell über die Codemagic-Weboberfläche bestätigt (Commit `846d3c4`, s. Punkt 5). Der finale Commit (Feature 5 Teil B) ist gepusht, seine "bestätigt grün"-Bestätigung steht noch aus.
  1. **Preisalarm-Benachrichtigung (PRICE_DROP)**: Backend verschickt bereits automatisch eine `Notification` mit `type: "PRICE_DROP"` und `actorName` als Prozent-String (z. B. `"-25%"`), wenn der Preis einer favorisierten Anzeige fällt — kein neuer Endpunkt nötig. `NotificationsView.swift` bekam einen neuen `PRICE_DROP`-Fall in `template` (nutzt den bereits vorhandenen i18n-Key `notifications.price_drop` mit `{name}`-Interpolation) und im Klick-Routing (`route(_:)`), gruppiert mit `SAVED_SEARCH_MATCH` (beide navigieren zur Anzeigendetailseite über `listingId`).
  2. **Verkaufs-Trichter (Conversion Funnel)**: neuer Endpunkt `GET listings/{id}/funnel` (Eigentümer oder ADMIN) liefert `{ views, favorites, contacts, offers, offersAccepted }`. Neue `FunnelDto` (`ListingModels.swift`) + `ListingRepository.getFunnel(id:)`. In `MesAnnoncesView.swift`s bereits vorhandenes Statistik-Panel (14-Tage-Balkendiagramm aus Phase 6) als zusätzlicher Abschnitt eingebaut: 5 Zeilen (Vues/Favoris/Contacts/Offres reçues/Offres acceptées), jede eine Label+proportionale Balkenbreite (relativ zu `views`, mirrort Webs `funnelPct()`)+Rohzahl. `MesAnnoncesViewModel.toggleStats()` lädt Funnel und View-Stats jetzt parallel (zwei unabhängige, sich nicht blockierende `Task`s), beide scheitern still bei Fehler wie schon die View-Stats zuvor.
  3. **ID-Verifizierung (kostenloses KYC-Lite)**: Nutzerseite in `ProfilView.swift` — neue Karte mit zwei `PhotosPicker`-Fotoauswahlen (Ausweisfoto, Selfie, mirrort das Avatar-Upload-Muster aus Phase 8/Tranche 9), Upload über den bestehenden `POST upload`-Multipart-Endpunkt mit `type=idVerification` (neuer Preset-Wert, `UploadRepository.uploadImages()` unterstützte beliebige `type`-Strings schon seit Tranche 9), danach `POST auth/id-verification` mit `{idImageUrl, selfieImageUrl}`. Status-Abfrage über `GET auth/id-verification` (liefert `null` bei nie eingereicht, sonst `{status, createdAt, adminNote}` — decodiert als `IdVerificationStatusDto?`, da Swifts `JSONDecoder` einen JSON-`null`-Body direkt in ein `Optional<T>` decodieren kann, ohne `T.init(from:)` je aufzurufen). Vier UI-Zustände (nicht eingereicht/ausstehend/genehmigt/abgelehnt-mit-Notiz-und-Neueinreichung-erlaubt), mirrort exakt Webs `profil.component.html`-Kartenstruktur. Admin-Seite: neuer "Vérifications"-Tab in `AdminView.swift` (Segmented-`Picker` zum Umschalten Signalements/Vérifications, lazy geladen beim ersten Öffnen), der die bestehende Reports-Sektion 1:1 nachbaut — gleiche Filter-Pillen-Optik (jetzt mit eigenem Status-Set PENDING/APPROVED/REJECTED/ALL), gleiches `.alert(...)`-mit-eingebettetem-`TextField`-Notiz-Muster, gleiches "nur geänderte Felder aus der PATCH-Antwort mergen"-Vorsichtsmuster (`AdminViewModel.confirmIdVerificationAction()`, analog zu `confirmAction()`s Reports-Merge). Ausweis-/Selfie-Fotos als `AsyncImage`-Thumbnails (rohe Cloudinary-URLs, kein Auth-Header nötig). Neues `idVerified: Bool` (mit dem üblichen `decodeIfPresent(...) ?? false`-Muster) parallel zu `emailVerified`/`phoneVerified` auf `UserDto`, `ListingUserDto`, `ChatUserDto`, `SellerProfileDto` ergänzt; `VerifiedBadge` (`CommonComponents.swift`) um eine dritte Pille erweitert und an allen 4 bestehenden Einbindungsstellen (`SellerProfileView`, `ListingDetailView`, `ChatView`, `ChatListView`) den neuen Parameter mitgegeben.
  4. **Bildersuche ("Rechercher par photo")**: neuer öffentlicher Endpunkt `POST listings/search-by-image` (kein Auth nötig, Multipart-Feld `image` **singular**, nicht das allgemeine `images`-Feld von `POST upload` — serverseitig lokales Perceptual-Hashing, keine bezahlte Vision-API), liefert ein rohes `[Listing]`-Array nach Ähnlichkeit sortiert. `APIClient.upload()` bekam einen neuen optionalen `query`-Parameter (für ein mögliches künftiges `?country=`, aktuell ungenutzt — Web selbst schickt auch keinen Country-Filter mit, hier bewusst 1:1 Web-Verhalten übernommen statt der im Auftrag erwähnten Endpunkt-Möglichkeit vorzugreifen). Neue `ImageSearchView.swift`/`ImageSearchViewModel.swift`: `PhotosPicker` löst sofort den Upload aus, Ladezustand, danach 2-spaltiges `ListingCardView`-Grid oder Empty-State ("Aucune annonce similaire trouvée"). Einstieg: neuer Kamera-Icon-Button direkt neben `HomeView`s Such-Einstiegsfeld (iOS hat kein Navbar-Suchfeld zum Einbetten wie Web, daher als eigenständiger Button danebengesetzt), neuer `Route.imageSearch`-Fall.
  5. **Maskierte In-App-Sprachanrufe (WebRTC)**: Zweiteilig umgesetzt, wie in der Aufgabenstellung selbst vorgegeben, um nicht mehrere teure Codemagic-Wartezyklen an einer fehlschlagenden Paketauflösung zu riskieren. **Teil A** (eigener Commit `846d3c4`): `stasel/WebRTC` (SPM-Paket, vorgebautes Google-WebRTC-XCFramework, kostenlos, kein Signup/API-Key) zu `project.yml` ergänzt (`exactVersion: 153.0.0`, bewusst nicht `from:`, da die Major-Version bei diesem binären Paket schlicht die Chromium-Release-Nummer ist), `NSMicrophoneUsageDescription` ergänzt, `codemagic.yaml`s `max_build_duration` 45→60 Minuten, plus ein minimaler echter API-Aufruf zum Erzwingen eines tatsächlichen Kompilier+Link-Checks (nicht nur `-resolvePackageDependencies`) — **vom Nutzer als Codemagic-grün bestätigt**, bevor mit Teil B fortgefahren wurde, exakt wie vom Auftrag verlangt. **Teil B** (dieser Commit, siehe unten): die eigentliche Anruf-Logik + UI. Neue `Networking/CallManager.swift` (`@MainActor final class CallManager: NSObject, ObservableObject`, mirrort exakt das Architekturmuster aus Tranche 19s `LocationService.swift` — `nonisolated`-Delegate-Methoden + `Task { @MainActor in ... }`-Hops für alles, was `@Published`-Zustand anfasst, da `RTCPeerConnectionDelegate`-Callbacks auf WebRTCs eigenem Hintergrund-Thread feuern, nicht auf dem Main-Thread). **Ohne lokalen Compiler besonders sorgfältig recherchiert statt aus dem Gedächtnis geraten**: da `RTCSessionDescription`/`RTCIceCandidate` keine eingebauten String↔Enum-Konvertierungshelfer mitbringen, wurden alle API-Aufrufe (Factory-Erzeugung, Audio-Track-Erzeugung, Offer/Answer-Erzeugung, `setLocalDescription`/`setRemoteDescription`, ICE-Kandidat hinzufügen, die vollständige `RTCPeerConnectionDelegate`-Methodenliste inkl. der 9 nicht-optionalen Pflichtmethoden) direkt gegen `stasel/WebRTC-iOS`s eigene Referenz-Demo-App (`WebRTCClient.swift`/`SessionDescription.swift`/`IceCandidate.swift`, per `curl` von GitHub gelesen) verifiziert, nicht geraten. `ChatSocketManager.swift` bekam die 4 neuen Events (`call_offer`/`call_answer`/`call_ice_candidate`/`call_end`) inkl. Emit-Methoden — SDP/ICE-Kandidat reisen als verschachtelte Dictionaries, exakt im selben JSON-Shape wie der Browser sie nativ erzeugt (`RTCSessionDescriptionInit`/`RTCIceCandidateInit`), für Cross-Plattform-Interop mit Web/Android. `ChatViewModel.swift` leitet die 4 neuen Socket-Events an `CallManager.shared` weiter (`observeSocketEvents()`), neue `startCall()`-Passthrough-Methode. `ChatView.swift`: Anruf-Button (`phone.fill`) im Header, nur sichtbar wenn `!messagingBlocked() && callManager.state == .idle` (exakt Webs Gate), `IncomingCallOverlay` als `.fullScreenCover` (Anrufer-Initiale-Avatar, Name, Annehmen/Ablehnen), `CallBar` über der Nachrichtenliste bei `.outgoing`/`.active` (Klingelt.../In Kommunikation-Label, Stumm-Toggle nur wenn aktiv, Auflegen-Button), Fehler-Banner für `mic_denied`/`failed` (i18n-Keys `chat.call_*`, bereits durch den Tranche-23-i18n-Resync vorhanden). **Bewusste Einschränkung, geerbt von Phase 5s Chat-Architektur**: `ChatSocketManager.onEvent` hat nur einen Listener, während ein `ChatView` aktiv ist (nicht global ab Login wie bei Web), daher kann ein eingehender Anruf nur empfangen werden, während der Empfänger bereits genau diese Konversation geöffnet hat — nicht von irgendwo in der App wie bei Web oder einem echten Telefon-Dialer. Bewusst akzeptiert statt die gesamte Socket-Lebenszyklus-Architektur für dieses eine Feature umzubauen; dokumentiert hier statt überraschend stillschweigend eingeschränkt zu sein.
  - **Alle 6 Sprachdateien resynchronisiert** (`Resources/i18n/*.json`, 1569→1623 Zeilen, 1:1 aus `soukmar/src/assets/i18n/` kopiert) — bringt `notifications.price_drop`, `mes_annonces.funnel_*`, `profil.id_verification_*`, `admin.id_verifications_*`/`tab_id_verifications`/`reports_status_approved`/`reports_status_rejected`/`id_verification_note_prompt`, `image_search.*`, `seller.id_verified_short`, `chat.call_*` sowie alles andere seit Tranche 20 neu Hinzugekommene mit.
  - **Commits**: `a2b77ba` (Features 1–4: Preisalarm, Trichter, ID-Verifizierung, Bildersuche + i18n-Resync), `846d3c4` (Feature 5 Teil A: WebRTC-Paket + minimaler Integrationstest — **Codemagic-Build bestätigt grün**), `16cd943` (Feature 5 Teil B: vollständige Anruf-Logik + UI) und `4de85ed` (Fix für `16cd943`, s. u.). **`16cd943` schlug fehl** — erster Codemagic-Build-Fehlschlag der gesamten iOS-Historie: Codemagic zeigte nur die verkürzte Fehlerzusammenfassung ohne die eigentliche `error:`-Diagnosezeile (in einem eingeklappten, nicht kopierbaren Panel), aber `CallManager.swift` war als eigener fehlgeschlagener Compile-Befehl separat aufgeführt (neben dem großen Datei-Batch), was stark auf diese Datei als Ursache hindeutete. Per sorgfältigem manuellem Gegenlesen **zwei echte API-Signatur-Fehler** gefunden, indem diesmal nicht nur `stasel/WebRTC-iOS`s Referenz-Demo-App (von 2018/2019), sondern direkt die AKTUELLEN WebRTC-Header von `webrtc.googlesource.com` gegengeprüft wurden: (1) `RTCPeerConnectionFactory.peerConnection(with:constraints:delegate:)` ist im aktuellen Header `nullable` (bridged zu Swift `RTCPeerConnection?`), aber `makePeerConnection()` hatte einen nicht-optionalen `RTCPeerConnection`-Rückgabetyp und gab das nullable Ergebnis direkt zurück — Typkonflikt. (2) `RTCAudioSession`s einparametrige `setCategory(_:)`-Überladung (aus der alten Demo übernommen) existiert im aktuellen Header gar nicht mehr — nur noch `setCategory:mode:options:error:` und `setCategory:withOptions:error:`. Beide durch Commit `4de85ed` behoben (`makePeerConnection()` gibt jetzt `RTCPeerConnection?` zurück, `startCall()`/`acceptCall()` entpacken via `guard let` mit `callError = "failed"`-Fallback bei `nil`; `setCategory(_:withOptions:)` mit leeren Optionen). **`4de85ed` schlug ebenfalls fehl** — diesmal lieferte der Nutzer das komplette Rohlog als Datei, sodass die exakten Diagnosezeilen sichtbar waren: `setCategory`/`setMode` erwarten `AVAudioSession.Category`/`.Mode` (die Swift-nativen Enum-Typen) direkt als Parameter, nicht deren `.rawValue` (String) — beide Aufrufe hatten fälschlich `.rawValue` angehängt (aus der alten Demo übernommen, dort aber noch mit der inzwischen entfernten String-basierten Überladung kompatibel). Behoben durch Commit `ac2480c` (`.rawValue` auf beiden Zeilen entfernt; restliche Datei nach weiteren `.rawValue`-Fehlnutzungen durchsucht, keine gefunden). **Zwei-Fehlschläge-Lektion**: bei einem binären SPM-Paket ohne lokalen Compiler reicht "gegen die aktuellen Header verifizieren" allein nicht – auch scheinbar triviale Enum-vs-String-Bridging-Details (wo eine ObjC-`NS_TYPED_ENUM`-typisierte Konstante in Swift zu einem eigenen Typ statt zu `String` wird) lassen sich beim Gegenlesen leicht übersehen; das vollständige, unverkürzte Codemagic-Rohlog (statt der von der Codemagic-UI gekürzten Zusammenfassung) war beim zweiten Fehlschlag der entscheidende Unterschied, um den exakten Fehler statt nur die betroffene Datei zu kennen. **Codemagic-Build-Status für `ac2480c`: ausstehend** — `gh` hatte in dieser Session kein gespeichertes GitHub-Token (kein `swift`/`swiftc`/`xcrun` lokal verfügbar zum Gegenprüfen), Build-Status musste deshalb vom Nutzer über die Codemagic-Weboberfläche bestätigt werden. Ein echtes Durchklicken (Foto-Upload-Flow, Admin-Review, Bildersuche-Ergebnisse, und insbesondere ein echter Zwei-Geräte-Anruftest) steht ohnehin mangels Simulator/Gerät wie bei jeder iOS-Tranche weiterhin aus — ein WebRTC-Anruf lässt sich ohnehin grundsätzlich nicht im Simulator allein testen (braucht zwei echte Mikrofone/Geräte), das bleibt auch mit einem eingerichteten Apple-Developer-Konto ein manueller Zwei-Geräte-Test.

---

## Wichtige Dateipfade

| Was | Pfad |
|-----|------|
| XcodeGen-Spec | `project.yml` |
| CI-Konfiguration | `codemagic.yaml` |
| App-Einstieg | `SoukMar/SoukMarApp.swift` |
| Root-Navigation (Login/Home-Switch) | `SoukMar/Views/RootView.swift` |
| Networking | `SoukMar/Networking/APIClient.swift` |
| Session-Speicher | `SoukMar/Persistence/TokenStore.swift` |
| Demo-Accounts | `ahmed@soukmar.ma` / `fatima@soukmar.ma` / `youssef@soukmar.ma` (Passwort je `soukmar123`) — `youssef` ist ADMIN |

## Testen

Kein lokaler Emulator/Simulator verfügbar (kein Mac). Ablauf nach jeder Änderung:
1. Lokal nur auf offensichtliche Swift-Syntaxfehler prüfen (kein echter Compiler verfügbar) — vorsichtiger/genauer schreiben als bei Android, wo ein lokaler Gradle-Build sofort Fehler zeigt.
2. Committen + pushen zu `github.com/azizennajih/soukmar-ios`.
3. Codemagic-Build abwarten (Nutzer startet ihn aktuell manuell in der Codemagic-UI, bis Auto-Trigger eingerichtet ist) — Ergebnis vom Nutzer erfragen oder, falls ein Codemagic-API-Token hinterlegt wird, später automatisiert abfragen.
4. Bei Fehlern: Fix, erneut committen/pushen/prüfen — **nicht als "fertig" melden, ohne dass ein Codemagic-Build grün war.**
